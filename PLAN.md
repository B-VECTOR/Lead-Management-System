# LMS — Gap Analysis & Implementation Plan

> Reconciliation of the current codebase against the two **authoritative** reference documents named in `CLAUDE.md`: `LMS_PRD_updated.md` (PRD) and `LMS_Technical_Requirements_updated.md` (Tech Req, **v12**). A third doc, `specs.md` (**v0.6**), is **deprecated** — the frontend was built against it, and it diverges materially from the two updated docs. Per the decisions below, the two updated docs govern; `specs.md` is to be archived.

## PRD Update — Reconciliation (v1.0 → v3.0, reviewed 2026-07-16)

The uncommitted update to `LMS_PRD_updated.md` bumps the version 1.0 → 3.0. The 796-line diff is **mostly line re-wrapping**; the semantic changes are itemized below (P1–P17), then mapped to *already satisfied* / *needs code* / *decisions*. The companion **Tech Req doc was updated the same day to v16.0** (changelogs v13–v16) and was reviewed 2026-07-16 — it matches the PRD changes and adds implementation detail (see the Tech Req subsection after list A). All conflicts in section D were **resolved with the user 2026-07-16** (recorded there). **No code has been changed yet — implementation awaits explicit go-ahead on the proposed Phase 14/15 breakdown at the end of this section.**

### A. What changed in the PRD

- **P1 (§5.1)** "Role" → **Roles multi-select** via Django Groups, no `role` column, any-match permissions.
- **P2 (§5.1)** Employee ID must be **unique**; duplicates rejected with a friendly message.
- **P3 (§4.2, §5.8)** **Dropped** is set via a popup capturing an optional **drop remark, stored on the lead and shown on its detail page**; On Hold's popup-with-remark is formalized.
- **P4 (§5.2)** Marketing-created leads store `assigned_to` as plain **NULL** — no "Not Assigned" value persisted; label rendered by the app.
- **P5 (§5.3, workflow table)** **Tasks 7 & 8 gain a new field** "Has project moved to the next stage?" (Yes/No, shown + required only when Re-presentation = No) and **three-way close routing**: re-pres Yes → Task 8; No + moved Yes → **Tasks 9 and 10 open simultaneously**; No + moved No → **Task 17 opens directly** (short-circuit). Implication: Task 9 becomes a leaf (it no longer opens Task 10).
- **P6 (§5.4)** Checklist tickmark is directly clickable — toggles complete ⇄ not_started (edit popup keeps the 3-state form).
- **P7 (§5.5 rule 6)** Save-as-Draft / Save-&-Complete both "return the user to the **My Tasks page**"; validation errors must reference the field's **display label, never internal field names**.
- **P8 (§5.5 rule 7 — new)** Tasks routed around by a branch (e.g. 6–9 when SB = No; 14–16 when Extension = No) get a **Skipped status**; task lists show only open/hold/closed + skipped — **pending tasks are hidden**.
- **P9 (§5.7)** Allocation form: **Project Member 1–10** (was 1–5); the "(TBD is allowed for White)" note is dropped.
- **P10 (§5.7)** **Under-allocation amber indicator** — shown on submitted records and live inside the allocation form (alongside the existing red over-allocation flag).
- **P11 (§5.7)** **Allocation auto-close rework**: Implementation auto-closes when the **first Task 16 closes** (superseded) — or at lead Complete if never extended; each Extension closes when the **next cycle's Task 16 closes**; only the current cycle's allocation is ever Open; a **new Extension allocation is prefilled with the previous cycle's resources**. (Was: Impl + all Extensions close together at Task 17.)
- **P12 (§5.8)** **Unhold** also captures an optional remark; remarks are stored **per hold/unhold cycle** (and on the lead for drops), appended to the activity log, and **displayed as a banner** on Lead/Task detail while held (drop remark on dropped leads); a lead-level hold/unhold **copies its remark onto the task holds** it creates/releases.
- **P13 (§5.8)** One **"Hold Items" menu** containing Hold Leads + Hold Tasks views (was two separate sidebar menus).
- **P14 (§5.17)** Every reference table gains a **status column (active/inactive, default active)**; only active rows offered in dropdowns. Belt is now spec'd as **id / name / order / status** (order drives dropdown sort; Belt has no code).
- **P15 (§5.18 — new section)** Leads list **Tracker column** (closed vs total task *instances* + %, **skipped excluded**, extension/repeat cycles add instances, bar coloured by lead status, "Not started" before workflow start) and a **per-column filter row** (free-text: Company/Project, Project ID; dropdowns: Industry, Domain, Owner incl. "Not Assigned", Current Task sorted by task no., Status; options from on-screen data; AND-combined; "Clear filters").
- **P16 (§7 NFR)** **Audit columns on every table** — `created_by`, `created_on`, `updated_by`, `updated_on` (updated_* nullable, filled on every update, timestamps automatic).
- **P17 (removed text)** The two **dated Phase-13 override notes are gone**: §5.5 no longer says Brown + White(s) edit / Red is view-only, and §5.7 reverts to "assigned to the **Execution Red** selected by the Resource Manager" (workflow table cells back to "Edit: execution red"). See conflict D1.

### A2. Tech Req v12 → v16 review (2026-07-16)

The Tech Req update carries the same changes as P1–P17 (its changelogs v13–v16 map onto them) and settles details the PRD leaves open — these govern the implementation:

- **Hold/unhold/drop remarks (P3/P12):** exact columns — `lead_hold.remark` + `lead_hold.unhold_remark`, `task_hold.remark` + `task_hold.unhold_remark`, `leads.drop_remark`. Hold **and unhold** each popup an optional remark; the existing hold/unhold/drop endpoints accept `{"remark": "..."}`; lead-level actions copy the remark onto the task-hold rows; remarks append to the activity-log description; active hold remark = **amber banner** on Lead/Task detail, drop remark = **red banner** on dropped leads; the hold-history API returns both remarks per cycle.
- **Skipped semantics (P8):** `skipped` is set when a branch routes around tasks (5 = No → 6–9; 7/8 = No re-pres → 8; 13 = No → 14–16) **and on lead completion, any still-`pending` tasks also become `skipped`** (Task 17's on-close sweep).
- **Drop → task status:** §4.3.2 says a drop moves all open/hold tasks to `dropped` — a status §4.4's list omits. **Resolved with the user 2026-07-16: add a sixth `dropped` task status** (see D7).
- **Engine (P5):** the Tech Req explicitly requires **multi-condition (AND) branch support** in the workflow JSON — so the engine's `when` should accept a list of `{field, equals}` conditions, even though the 7/8 rules could be expressed with ordered single conditions alone.
- **Tracker/filters (P15):** API shape is `task_progress {total, closed, percent}` on the lead serializer; "Clear filters" lives in the page header; empty results keep the filter row visible with a "No leads match the filters" message.
- **Resource module (P9/P10/P11):** the Extension **prefill happens when the row is created at Task 15** (from the Implementation row on the first extension, the previous Extension row afterwards); under-allocation shows on **submitted rows and live in the form**; Task 16's on-close closes the **superseded previous** row; Task 17 closes only the current one. `white`'s "TBD allowed" note is removed.
- **Audit columns (P16):** §4.0 defines the shape — `created_by` nullable for system-generated rows (semantic creators like `leads.created_by` stay required), `updated_by`/`updated_on` nullable, and **existing `created_at`/`updated_at` fields are renamed `created_on`/`updated_on`**.
- **Belts:** seeded `order` 1–9 in the listed sequence; `ORDER BY order, name`.
- Marketing NULL-`assigned_to`, Groups-based roles, unique `employee_id`, checklist tickmark toggle, and the "Hold Items" menu are all restated identically to the PRD (P1, P2, P4, P6, P13).
- The Tech Req also removes the Phase-13 override note in §6 — consistent with decision D1 (Red executes).

### B. Already satisfied by existing code

- **P1** — Decision #2 / Phase 2 (Groups M2M, no role field, any-match group checks throughout `permissions.py`).
- **P2** — `employee_id` is already `unique=True` ([models.py:58](backend/authentication/models.py#L58)). *Verify only:* that the duplicate-ID API error surfaces as a friendly message in the user form.
- **P4** — Phase 3: `assigned_to` is a nullable FK; "Not Assigned" is a render-time label only.
- **P3 (hold half)** — hold popup + optional remark shipped in Phase 11b; remark stored per hold interval (`HoldRecord.reason`, Phase 13f) and the lead-hold cascade already copies it to task holds ([holds.py:85](backend/leads/holds.py#L85)). *The drop half is not built — see C3.*
- **P6** — already built: `ChecklistItemRow` has a direct Checkbox toggling complete ⇄ not_started ([ChecklistItemRow.jsx:85-99](frontend/src/components/leads/ChecklistItemRow.jsx#L85-L99)).
- **P14 (order half)** — `Belt.order` already exists (noted in §2b as a then-undocumented extra; now official). *Status column not built — see C9.*
- **P15 (partial)** — the Leads list already has a Progress bar column fed by `progress` = closed/total tasks (Phase 10a) and a global search + status dropdown. The full §5.18 spec (per-column filter row, Current Task filter, status-coloured tracker, "Not started" state, skipped exclusion) is not built — see C10.

### C. Requires code changes

1. **P5 — Task 7/8 re-branching.** [workflow_data.py](backend/leads/workflow_data.py): add `moved_to_next_stage` (`required_when` re-pres = No) to tasks 7 & 8; routing becomes ordered rules `Yes→[8]`, `moved=No→[17]`, default `→[9,10]`; Task 9's routing `[10]` → `[]`. Parallel opens already work (`[4,5]`), but per Tech Req §4.11 the engine's `when` must additionally support a **list of AND-ed conditions** ([engine.py:288-294](backend/leads/engine.py#L288-L294)). Re-seed via `seed_workflows`; update routing tests + the Phase-4 walk tests (9 no longer opens 10). Frontend conditional show/require already handles `required_when`.
2. **P8 — Skipped + dropped statuses.** `Task.Status` gains `SKIPPED` and `DROPPED` ([models.py:164](backend/leads/models.py#L164)); engine materializes skipped instances when a branch routes around tasks (today tasks are created lazily, so "skipped" rows never exist) and sweeps remaining `pending` → `skipped` at lead completion; dropping a lead flips open/hold tasks → `dropped` (D7); task lists/steppers hide `pending`, show skipped; tracker denominator excludes skipped (P15). Per D4, this supersedes Phase 13e's pending-task visibility in lists (the "Opens on <date>" hint is dropped with them).
3. **P3 — Drop remark.** New field on `Lead` (+ migration); drop flow becomes a popup (today Dropped is a plain option in the LeadDetail status dropdown, [LeadDetail.jsx:32](frontend/src/pages/LeadDetail.jsx#L32)); serializer/endpoint accepts the remark; append to activity log; render remark on the detail page for dropped leads.
4. **P12 — Unhold remarks + banners.** `HoldRecord` has one `reason` per interval (hold side only) — add an unhold remark (new field + [holds.py](backend/leads/holds.py) signatures + hold/unhold endpoints + popups); lead-unhold copies it to task unholds; show the **active hold remark as a banner** on Lead/Task detail (trail data already exposed by Phase 13f serializers).
5. **P13 — Hold Items menu.** [Sidebar.jsx](frontend/src/components/layout/Sidebar.jsx#L19-L20): group the two entries under one "Hold Items" item (frontend-only).
6. **P9 — Project Member 1–10.** `ResourceAllocation` gains `project_member6`–`10` (+`SINGLE_RESOURCE_FIELDS`, migration), serializer fields, and the allocation dialog in [Resources.jsx](frontend/src/pages/Resources.jsx). Clarify the dropped "(TBD allowed for White)" note — see D1 interaction.
7. **P10 — Under-allocation flag.** Model/serializer property (per-type, mirroring `is_over_allocated`, [models.py:512](backend/leads/models.py#L512)) + amber indicator on the Resources list and live in the allocation form.
8. **P11 — Auto-close rework + prefill.** [resources.py](backend/leads/resources.py): on Task 16 close, auto-close the superseded Implementation/previous-Extension row; `ensure_allocation_row` prefills a new Extension row from the previous cycle (at Task 15 open); adjust `on_close` hints in `workflow_data.py`; T17 closes only the final current row. Simplified by D1: the Phase-13 `latest_task_assignee`/`current_allocation_editor_ids` machinery is being reverted anyway (C13).
9. **P14 — Reference-table status.** Add `status` to `Country`/`Industry`/`Area` ([reference/models.py](backend/reference/models.py)) and `Belt` ([authentication/models.py](backend/authentication/models.py)) + migrations + admin; list endpoints filter to active; keep PROTECTed FKs valid for existing leads (inactive ≠ deleted).
10. **P15 — Leads list tracker + column filters.** [LeadsList.jsx](frontend/src/pages/LeadsList.jsx): filter row under headers (text: Company/Project, Project ID; dropdowns: Industry, Domain, Owner incl. Not Assigned, Current Task, Status; AND; Clear filters); tracker bar coloured by lead status + "Not started" state. Backend: `LeadSerializer` needs a `current_task` (lowest open task no.) field; `progress` denominator must exclude skipped once C2 lands.
11. **P16 — Audit columns everywhere.** Cross-cutting: an abstract audit base (`created_by/created_on/updated_by/updated_on`) applied across `authentication`, `reference`, and `leads` models + migrations, with `updated_by` plumbed from the request user at the serializer/service layer. Large, touch-everything change — propose scheduling as its own phase.
12. **P7 — Validation-error labels.** Engine errors are keyed by internal field key with generic text ([engine.py:236](backend/leads/engine.py#L236)); error text becomes label-based (e.g. `"Expected start date of next stage" is required.`) and any surface that leaks raw keys (toasts) is fixed. The "return to My Tasks page" half is **void per D3** — no such page; buttons keep the user on the lead's task view.
13. **D1 — Revert the Phase-13 execution-edit model.** Execution tasks go back to **Execution Red as assignee/editor**: `workflow_data.py` execution-task `assignee` `execution_brown` → `execution_red`; [engine.py](backend/leads/engine.py) `_resolve_assignee`; [permissions.py](backend/leads/permissions.py) drops the Brown/White co-editor + allocation-people view rules (13a) back to assignee-edits / allocation-independent visibility; `submit_allocation` keeps **Red mandatory** (12b) but drops the separate editor (Brown/≥1-White) requirement; re-seed; rewrite the Phase-13a tests. The docs already read this way after the update — the repo's dated override notes were removed in both files.

### D. Decisions (resolved with the user 2026-07-16)

1. **Follow the updated PRD — Execution Red executes.** The Phase-13a override (Brown + Whites edit, Red view-only) is **rescinded**; revert per C13. This also settles C6's TBD-White wording (note simply removed) and simplifies C8.
2. **Follow-ups keep the Phase-12 broadening.** Anyone who can view a lead may raise a follow-up on it — the code stands as-is; the docs' LM-only wording (§5.11 / §6 matrix / Tech Req §8) is superseded on this point. *(Doc-note pending — see Phase 14g.)*
3. **No "My Tasks" page.** The "return to the My Tasks page" sentence in both docs is a stale reference — no such screen will be built; Save-as-Draft / Save-&-Complete keep the current navigation.
4. **Follow the updated PRD on pending tasks** — hidden from task lists; `skipped` status shown. Supersedes Phase 13e's pending-task "Opens on <date>" list treatment.
5. **Tech Req updated to v16** the same day and reviewed (section A2) — the two governing docs agree again. Residual hygiene: PRD §1/§9 again cite `LMS_Technical_Requirements.md` (the real file is `..._updated.md`) — re-fix alongside Phase 14g.
6. **P11 knowingly rewrites verified Phase-6 behaviour** (all-close-at-T17 → per-cycle handover) — accepted as the new spec.
7. **Dropped lead → tasks get a new `dropped` status.** Resolves the Tech Req's §4.3.2 vs §4.4 contradiction: a sixth task status `dropped` is added; a lead-drop flips its open/hold tasks to it.

### Implementation breakdown

- **Phase 14 — PRD v3 / Tech Req v16 alignment** — **Completed 2026-07-16** (see the Phase 14 section in §3).
- **Phase 15 — audit columns on every table** (C11): abstract audit base, `created_at`/`updated_at` → `created_on`/`updated_on` renames, `updated_by` plumbing through serializers/services. Kept separate — it touches every model and migration in the project. **Not started — awaiting go-ahead.**
- **Phase 16 — short-close cascade + banner** — **Completed 2026-07-21.** Requested directly by the user, independent of Phase 15 (no file overlap). Short-closing a project cycle now sweeps whatever task is currently `open`/`hold`/`pending` under the lead to `skipped` immediately (`Task.short_closed` boolean marks these rows, no new `Task.Status`/`Lead.Status` value added), stamps the current `project_details` row (`short_closed`/`short_closed_at`/`short_closed_by`, scoped per-cycle since short-close already only ever acted on `is_current`), and surfaces it as: a blue banner on Lead detail (`LeadSerializer.short_close_info`, retires once the lead reaches `Complete`), a distinct "short-closed" message on the swept task's own view, and a "Short-closed" label replacing the Yes/No control on the Project Closure screen once triggered. `checklist`/tests: `backend/leads/tests.py::ShortCloseCascadeTests` (6 tests); full `leads` suite (171) green. PRD §5.12 and Tech Req §4.4/§4.8/workflow-table-row-17/§9.2 updated to match. **2026-07-21 follow-up:** the same `short_close_info` (already on `LeadSerializer`, so no backend change needed) now also renders as a blue "Short-closed" pill in the Leads-list Status column (`frontend/src/pages/LeadsList.jsx`), next to the existing "Task on hold" pill — lets Lead Admin/Manager spot short-closed leads at a glance without opening each one. No new `Lead.Status` value, per the original decision.
- **Phase 16.1 — Short Closed status + compulsory remark** — **Completed 2026-07-21.** Requested by the user, reversing two of Phase 16's decisions after review. (1) A new terminal **`Short Closed`** value now exists on both `Lead.Status` (system-only, orange badge) and `ProjectDetails.Status`; short-close sets it immediately and it is *kept* — `_apply_on_close`/`projects.complete_current_cycle` redirect the terminal closure of a short-closed cycle from `Complete` to `Short Closed`, so a short-closed engagement is permanently distinguishable from a naturally-completed one (Task 17 still runs so fees are collected). (2) A remark is now **compulsory** on short-close (`ProjectDetails.short_close_remark`; the endpoint 400s without it) — mirrors the hold/unhold remark popup but required, so a project can't be short-closed by accident. Frontend: `ShortCloseButton` dialog on the Project Closure screen (Confirm disabled until a remark is typed), `Short Closed` styles in `StatusBadge.jsx`, remark surfaced in the Lead-detail banner. Confirmed with the user 2026-07-21: status lives on **both** the Lead and the closure-cycle badge; Task 17 still opens. Migration `0015`; `ShortCloseCascadeTests` updated + extended.

## 0. Decisions (resolved 2026-07-10)

All previously-open questions are now settled. Recorded here so future sessions don't re-litigate them.

1. **The two updated docs govern.** `specs.md` (v0.6) is old and superseded. The frontend's flattened model (Lead = unit of work, no Project entity, 9-step flow, 3 lead types) is rebuilt to the v12 model: `Company → Lead → Project → Task`, **17-task** BD workflow, `project_details` history, `resource_allocation`, Project-ID generation.
2. **Multi-role via Django Groups — keep.** Users can hold multiple roles (Groups M2M); no single-`role` field.
3. **`username` is restored per the docs.** PRD §5.1 / Tech Req §4.1 list a Username field, so the User model gains a `username` field used as the **login identifier** (SimpleJWT default); `email` remains a required field. *(This changes the currently email-based auth — see Phase 2 note.)*
4. **Keep Attachments, Activity log, and Notifications** in this build (they exist in the frontend today). These are wired to the backend in Phase 8 rather than dropped/deferred. (Finance role remains future scope per PRD §5.10.)
5. **Archive `specs.md`** and fix stale doc references (CLAUDE.md + PRD point at `LMS_Technical_Requirements.md`; the real file is `..._updated.md`). Handled in Phase 0.
6. **Two lead types only: BD and Mining.** Extension is a **workflow cycle inside BD** (Tasks 13–16), not a lead type. Drop the frontend's "Extension" lead type.
7. **`employee_id` and `mobile_no` become numeric (≥0)** per the docs (Phase 2). *(Caveat: numeric `mobile_no` cannot store a leading `+` or formatting — following the docs as instructed.)*
8. **New app for the reference tables** (`countries`, `industries`, `areas`) — not folded into `authentication`.

## 1. Current State Summary

**Working today (verified in code):**
- **Backend auth** — DRF SimpleJWT login/refresh/logout (with rotation + blacklist), self-service password change. Custom email-login `User` model with soft-delete. ([backend/authentication/](backend/authentication/))
- **Backend user management** — full CRUD API (`/api/users/`) gated by a real server-side permission class ([permissions.py](backend/authentication/permissions.py)), plus Django-admin-style template management pages, and a thorough test suite ([tests.py](backend/authentication/tests.py)).
- **RBAC scaffolding** — the 7 roles exist as Django **Groups**, seeded idempotently by `seed_lookups`; `belts` reference table seeded (9 belts). Only the `user_management` group has functional permissions; the other 6 groups are empty placeholders.
- **Frontend shell & auth** — React 19 + Vite + React Query + Tailwind v4 + shadcn/ui. Login, token storage/refresh, route protection, user management screens, and the roles/belts lookups are **wired to the live backend** and functional.
- **Frontend UI (mock-backed)** — a large, polished UI exists for Dashboard, Leads (list/form/detail with task stepper, checklist, files, activity, follow-ups, resources tabs), Follow-ups, Notifications — **but all of it runs on a localStorage mock DB**, not the backend.

**Not working / not built:**
- **The entire lead domain backend** — no `leads`, `tasks`, `checklists`, `workflows`, `resource_allocation`, `project_details`, `followups`, reference-table (country/industry/area) models exist. Only `authentication` app is present ([INSTALLED_APPS](backend/config/settings.py)).
- **The workflow engine** — no task-sequencing logic, no signals, no scheduler/Celery.
- **Everything the frontend shows beyond auth/users is illusory** against the real backend — it reads seeded mock records ([frontend/src/mocks/db.js](frontend/src/mocks/db.js), `api/client.js:5-8`).
- **Server-side RBAC beyond user management** — the 6 non-user-mgmt groups carry no permissions; nothing enforces the PRD §6 matrix server-side.
- **Forgot-password** — mocked in the frontend, no backend endpoint ([auth.js:194-221](frontend/src/api/auth.js#L194-L221)).

**Bottom line:** this is a *frontend-first prototype mid-migration*. Auth + users are cut over to Django; the whole lead-management domain still needs to be built backend-first and then re-wired — and the frontend domain model itself was built to a superseded spec (now being rebuilt to v12).

## 2. Gaps & Discrepancies

### 2a. Specified but not built (backend)
- Reference tables `countries`, `industries`, `areas` (Tech Req §4.2, §13.2–13.4) — **absent**. Only `belts` exists.
- `leads` + `companies` (Tech Req §4.3; PRD §5.2) — absent.
- `tasks`, `checklists`, `task_extra_fields` (Tech Req §4.4–4.6; PRD §5.3–5.5) — absent.
- `workflows` JSON table + task engine (Tech Req §4.11, §5; PRD §5.16) — absent.
- `workflow_trigger_config` + scheduled opener job (Tech Req §4.12; PRD §5.6) — absent.
- `resource_allocation` + auto-close rules (Tech Req §4.7; PRD §5.7) — absent.
- `project_details` history + Project ID generation (Tech Req §4.8, §13; PRD §5.15) — absent.
- `lead_hold` / `task_hold` + elapsed-time + Held views (Tech Req §4.9, §6; PRD §5.8) — absent.
- `followups` + "Other Tasks" screen (Tech Req §4.10, §8; PRD §5.11) — absent.
- Leads-funnel dashboard + activity log server-side (PRD §6, §12 matrix) — absent (frontend has a mock dashboard).
- Server-side enforcement of the PRD §6 / Tech Req §12 permission matrix — absent for all roles except User Management.

### 2b. Exists but diverges from the docs (all now scheduled for correction)
- **User model omits `role` and `username`, uses email login** ([models.py:46-88](backend/authentication/models.py#L46-L88)). Per Decisions #2/#3: keep Groups (no `role` field), but **restore `username`** (Phase 2).
- **`User.domain` is a `CharField`** ([models.py:74](backend/authentication/models.py#L74)); must become FK → `areas` (Phase 2, after Phase 1 tables).
- **`employee_id` / `mobile_no` are `CharField`** — become numeric ≥0 per Decision #7 (Phase 2).
- **`Belt` has an extra `order` field** ([models.py:17-21](backend/authentication/models.py#L17-L21)) — harmless addition; leave as-is.
- **Frontend lead domain is flattened & CRM-flavored** — no Project entity, `owner_id` + `assigned_to` split, extra fields (`priority`, `tags`, `conversion_reminder`, `source_detail`, `start_date`, `target_date`, `internal_notes`), **no `country` field**, industry/domain from hardcoded arrays ([LeadForm.jsx:16,21-26](frontend/src/pages/LeadForm.jsx#L16-L26)). Rebuilt to PRD §5.2 (Phase 3).
- **Frontend BD workflow is 9 steps, not 17** — rebuilt to 17 (Phase 4).
- **Frontend "Resources" is request-based** rather than the docs' allocation-task-driven `resource_allocation` — rebuilt (Phase 6).
- **"Extension" modeled as a lead type** — dropped per Decision #6 (Phase 3).

### 2c. Broken / inconsistent flows
- **Lead visibility breaks for real users:** `scope.js` reads the **mock** leads and matches seeded user ids that won't equal real backend ids after real login ([scope.js:19-29](frontend/src/api/scope.js#L19-L29)). Fixed when leads move server-side (Phase 3).
- **No route-level role guarding:** `RequireAuth` accepts a `roles` prop but `App.jsx` never passes it ([App.jsx:28](frontend/src/App.jsx#L28)); `/users` is only hidden from the sidebar. Addressed as screens are re-wired.
- **`RoleSwitcher` is a dead control** — `AuthContext.switchUser` is a no-op against real auth. Removed/settled in Phase 8.
- **RBAC unenforced server-side for 6 of 7 roles** — those Groups have no permissions; every new lead/task endpoint must add DRF permission classes + queryset scoping from day one (PRD §7).
- **No `/me` endpoint** — identity after reload is trusted from localStorage until a 401. Optional `/me` in Phase 8.
- **Doc/reference drift** — fixed in Phase 0.

## 3. Phased Implementation Plan

Ordered by dependency (backend-first where the frontend depends on it). Each phase is scoped to roughly one sitting. Cross-cutting rule for every backend phase: add DRF permission classes + queryset scoping per PRD §6 / Tech Req §12 as endpoints are built (never trust the frontend), and enforce the global validators (numeric ≥0, no past dates except `date_of_joining`) at the serializer level (Tech Req §3).

### Phase 0 — Doc hygiene (no app code)
Implements Decision #5. No dependencies.
- [x] Archive `specs.md` (e.g. move to `archive/specs_v0.6.md`) so no new code is built against it. *(`git mv specs.md archive/specs_v0.6.md`.)*
- [x] Fix stale references to `LMS_Technical_Requirements.md` → `LMS_Technical_Requirements_updated.md` in `CLAUDE.md` and `LMS_PRD_updated.md` §1/§9.
- [x] Optionally record the §0 decisions in CLAUDE.md's Conventions section. *(Added a compact "Settled decisions" section pointing to PLAN.md §0, per CLAUDE.md's "short and stable" rule.)*

### Phase 1 — Reference-data foundation
Implements Tech Req §4.2, §13.2–13.4; PRD §5.17. Decision #8 (new app). No dependencies.
- [x] New `reference` app: `Country`, `Industry`, `Area` models (`id/name/code`, name & code unique). *(Share an abstract `ReferenceEntry` base for the common shape; `Area` documents its "Domain" alias.)*
- [x] Migrations + Django admin registration for all three. *(`0001_initial`; registered via a shared `ReferenceEntryAdmin`.)*
- [x] Seed command (new, or extend `seed_lookups`): 2 countries, 16 industries, 11 areas with the codes in Tech Req §13.2–13.4. *(New `seed_reference` command in the `reference` app — kept separate from `authentication`'s `seed_lookups` per Decision #8. Idempotent via `get_or_create(name=...)`, mirroring the belt-seeding pattern.)*
- [x] Read-only DRF list endpoints: `/api/countries/`, `/api/industries/`, `/api/areas/`. *(`ListAPIView`, `pagination_class=None`, matching the existing `BeltListView`; default `IsAuthenticated` enforced.)*

### Phase 2 — User-model reconciliation
Implements Decisions #3, #7; Tech Req §4.1. Depends on Phase 1 (`Area` for the `domain` FK). **Completed 2026-07-10.**
- [x] Add `username` (unique, required) to `User`; set **`USERNAME_FIELD = "username"`** (login by username per docs); keep `email` as a required field. *(Restored by removing `username = None`, inheriting `AbstractUser.username` unchanged; `REQUIRED_FIELDS` now lists `email` + profile fields. Manager rewritten to `(_)create_user(username, email, password, …)`.)*
- [x] Convert `employee_id` and `mobile_no` to numeric (`≥0`, `MinValueValidator(0)`), dropping the string/regex forms. *(**Deviation:** used `PositiveIntegerField` / `PositiveBigIntegerField` — both ship a `MinValueValidator(0)` by default, so this satisfies "≥0, MinValueValidator(0)" idiomatically. `mobile_no` needs the 64-bit `PositiveBigIntegerField` because a 10-digit number overflows a 32-bit int. Removed the old `mobile_validator` regex.)*
- [x] Change `User.domain` from `CharField` to `FK → reference.Area`.
- [x] Data migration for existing rows (backfill `username`, coerce numeric fields, map `domain` text → `Area`). *(`0003_username_numeric_ids_domain_fk`. Coercion rules for the 4 dev rows, documented in the migration: username = email local-part (pk-suffixed on collision); employee_id = digits from old value, **falling back to the row's pk on empty/collision** since the source data was alphanumeric (`EMP-001`, `ADMIN-0001`) and not safely numeric — see note below; mobile_no = digits only (drops the one `+` prefix); domain = case-insensitive match against `Area.name`, else NULL. One existing value `'project'` matched no Area and was set NULL.)*
- [x] Update `UserSerializer`, `CustomTokenObtainPairSerializer`, admin, `forms.py`, and `seed_lookups` accordingly. *(Added `username` to serializer fields + token payload, all three forms, and admin fieldsets/list/search. **`seed_lookups` needed no change** — it only seeds Groups and Belts, touching no `User` fields. Also updated the `profile`/`manage/list` templates to show `username`.)*
- [x] Frontend: `Login` submits **username** (not email); `UserForm` adds `username` + Area-FK domain dropdown; retire hardcoded `INDUSTRIES`/`DOMAINS` from `mocks/seed`. *(Login + `AuthContext` + `api/auth.js` now use username; added `getAreas`/`useAreas` and pk↔name mapping for the domain FK (mirroring belts); `UserForm` gains a username field, numeric employee_id/mobile inputs, and a live Area-backed domain select. **Deviation:** the hardcoded `INDUSTRIES`/`DOMAINS` constants are **left in `mocks/seed.js`** because `LeadForm.jsx` (still mock-based) imports them; removing them now would break it. `UserForm` no longer uses them — full deletion happens in Phase 3 when `LeadForm` is rebuilt.)*
- [x] **Re-verify the full auth flow end-to-end** (login/refresh/logout/change-password + user CRUD) after the username switch — this phase touches currently-working code. *(Backend suite rewritten to the new model — **33 tests pass** (was 27; added username-required, duplicate-username, negative-employee_id, and a login-by-username `AuthTokenTests` suite). Also smoke-tested live over HTTP against `runserver`: login by username ✓, login by email correctly rejected (401), refresh ✓, change-password ✓, logout ✓, and a create/patch/delete user cycle incl. duplicate-username + negative-id 400s. Frontend `npm run build` + `lint` clean. Migration applied to the dev DB; seeds still idempotent.)*

**Note on employee_id coercion (for future reference):** the docs (§4.1, Decision #7) mandate numeric `employee_id`, but the existing dev rows held alphanumeric IDs (`EMP-001`, `ADMIN-0001`). There is no lossless numeric mapping (`EMP-001`/`ADMIN-0001` both reduce to `1`), so the migration falls back to the unique row pk on any empty/collision. If real employee IDs are alphanumeric in production, revisit whether `employee_id` should be numeric at all — but that reopens Decision #7 and is out of Phase 2 scope.

### Phase 3 — Company + Lead models & screens
Implements PRD §5.2, §5.9, §5.13, §6; Tech Req §4.3, §10, §11, §12. Depends on Phases 1–2. **Completed 2026-07-10.**
- [x] `leads` app: `Lead` model per Tech Req §4.3 (incl. `country/industry/domain` FKs, nullable `assigned_to`, `lead_type` = BD/Mining only, system `status`, `created_by`, `project_id`/`project_id_base`/`extension` placeholders). *(**Deviation — no Company entity:** the plan's "`Company` + `Lead` models" wording conflicts with the governing docs, which both model the company as a plain text field — Tech Req §4.3 lists `company_name | text`, PRD §5.2 lists `Company Name | Text`, and no `companies` table exists in the §4 data model. Per the docs-govern rule the company was built as a `company_name` CharField on `Lead`; **confirmed with the user 2026-07-10.** Reference FKs use `on_delete=PROTECT` (a lead must keep its country/industry/domain for Project-ID generation); `created_by` PROTECT; `assigned_to` SET_NULL + nullable = "Not Assigned".)*
- [x] Serializers + viewsets; DRF permission classes + querysets for Lead Manager (own), Lead Admin (all + assign owner to unassigned), Marketing (own, `assigned_to` hidden) — PRD §6 rows. *(`LeadListCreateView`/`LeadDetailView` generics + `LeadPermission` (group-name checks, not Django model perms) + `LeadQuerysetMixin` scoping. **Interpretation:** "Lead Manager (own)" reconciles §5.9 ("leads assigned to them") with §12 ("own **created** leads") as the **union** — `created_by == self OR assigned_to == self`. **No delete endpoint** — the §12 matrix defines no delete-lead action; a lead is cancelled via `status = Dropped` (RetrieveUpdate, not Destroy). **Added endpoint:** `/api/assignable-users/` (the lead form's owner dropdown needs BD users, but `/api/users/` is user-management-gated; read here as `lead_manager`-group members = "BD persons" per §4.3/§5.2, gated to LM/LA). "BD users" is otherwise undefined in the docs.)*
- [x] Status-flow guards (system-only transitions to Hybernation/Complete) — Tech Req §4.3.2. *(`Lead.SYSTEM_ONLY_STATUSES` + serializer `validate_status`; `status` forced to `In Progress` on create. On-Hold cascade to tasks is Phase 5; here On Hold/Dropped are accepted as plain status writes since no tasks exist yet.)*
- [x] Frontend: rewire `api/leads.js` to the real API; rebuild `LeadForm` to PRD §5.2 fields (add Country; drop CRM-only fields and the Extension lead type); fix `scope.js`/visibility to use backend data. *(**Scoped to a self-contained slice, confirmed with the user 2026-07-10** — the lead API is shared by Phase 4/6/7/8 screens still on the mock, so a full cutover was out of scope. Done: `api/leads.js` now hits `/api/leads/` (list/get/create/update + `assignLeadOwner`; owner/rep split collapsed to a single `assigned_to`, `archive` dropped for `status=Dropped`); `LeadForm` rebuilt to the 9 PRD §5.2 fields with live Country/Industry/Domain + assignable-owner selects; `LeadsList` re-columned to backend fields; `LeadDetail` reworked to the backend shape (single owner, status guard, Project-ID card). `scope.js` PERMISSIONS mirror the backend `LeadPermission`; its mock `visibleLeadIds`/`canViewLead` are **kept** for the not-yet-migrated followups/resources/dashboard mock modules. **Deviation — `api/companies.js` not "rewired":** the no-Company-entity decision means there is no company endpoint to wire to, so the module is marked deprecated (company is now a text field) and dropped from the Phase-3 screens rather than rewired. **Deferred (their phases):** `LeadDetail`'s Task/Activity/Files/Follow-up/Resources tabs, Dashboard, and FollowUpsList stay mock-backed and read empty for backend leads.)*

**Verification (Phase 3):** backend suite **57 tests pass** (24 new in `leads/tests.py` — create/owner rules, status guards, per-role visibility scoping, object-level edit rules, no-delete, assignable-users gating). Migration `leads.0001_initial` applies cleanly; `seed_lookups` + `seed_reference` idempotent. Frontend `npm run build` + `oxlint` clean. Smoke-tested live over HTTP against `runserver` (JWT): LM login-by-username → create-without-owner 400 / create-with-owner 201 / status=Complete 400 / scoped list; Marketing → owner forced Not-Assigned, PATCH owner 400, assignable-users 403; Lead Admin → sees all, assigns unassigned lead 200, create 403, edit-once-assigned 403. Smoke data removed afterward.

### Phase 4 — Workflow engine, Tasks, Checklists
Implements PRD §5.3–5.5, §5.16; Tech Req §4.4–4.6, §4.11, §5, §6. Depends on Phase 3. The core of the system. **Completed 2026-07-10.**
- [x] `workflows` JSON table + admin; seed the **17-task** BD workflow (assignees, checklist keys, extra-field schema, routing/branching) — Tech Req §5. *(`Workflow` model (name/type/workflow JSON/status) + `WorkflowAdmin`. The definition lives in [workflow_data.py](backend/leads/workflow_data.py) as plain data — checklist keys, per-field schema (`type`/`required`/`required_when`/`columns`), `assignee` rule, `is_allocation_task`/`allocation_type`, and ordered `routing` rules — seeded idempotently by the new `seed_workflows` command via `update_or_create(name,type)`. The JSON schema is documented at the top of that file. **Fork found in the source:** Task 3's notes and Task 5's both read "Opens after 3.6", so closing Task 3 opens **both** 4 and 5 in parallel (encoded as `"open":[4,5]`); Task 4 is a leaf, and the flow converges again at Task 10 (reached via 9, or via Task 5's "No" skip).)*
- [x] `Task`, `Checklist` models; extra fields as JSON on task (Tech Req §4.6). *(`Task` (lead FK, task_no, assigned_to, status `pending/open/hold/closed`, `is_allocation_task`, `extra_fields` JSON, opened/closed_at, nullable `elapsed_time` for Phase 5) and `Checklist` (item_key/label, status `not_started/inprogress/complete`, remark, last_edited_by/at). **No `unique(lead, task_no)`** — the 7/8 loop and 13→16 extension cycle deliberately create repeat instances per task number.)*
- [x] Task engine service: open Task 1 on `assigned_to` NULL→user (signal, Tech Req §4.3.1); Save-as-Draft vs Save-&-Complete; closure validation (all checklist complete + mandatory fields) → open next per routing; branch logic (Task 5 skip→10, 7/8 loop, 13→14/17, 13→16 extension loop). *([engine.py](backend/leads/engine.py) is a generic interpreter of the workflow JSON — no task numbers/sequencing hardcoded. Start via a `pre_save`/`post_save` signal on `Lead` ([signals.py](backend/leads/signals.py)) that fires when `assigned_to` goes NULL→user (covers LM-at-create, LA-assign, and admin edits); idempotent + guarded (BD, In Progress, no tasks yet). Draft-save = PATCH `extra_fields` (global rules only); Save-&-Complete = the `complete` endpoint (checklist-all-complete + mandatory fields, then routing). Global validators (§3: number ≥ 0, no past dates; `required_when` conditional requiredness) run on every write. All branches verified by tests: 5-No→10, 5-Yes→6, 7-Yes→8 / 8-loop / 8-No→9, 13-Yes→14→15→16→13-loop, 13-No→17, 17 terminal.)*
- [x] Task assignment/visibility rules + reassignment (Tech Req §6). *(`TaskPermission` + `can_view_task`/`can_edit_task`/`can_reassign_task`: view = assignee ∪ Lead Admin (all) ∪ Lead Manager (their leads) ∪ lead owner (view-only); edit = assignee while open; reassign endpoint reassigns to another active user. **Interpretation — who may reassign:** the docs say "any task can be reassigned" from the task view but don't name the actor; this build allows the current assignee (hand-off) and Lead Admin — documented in `can_reassign_task`.)*
- [x] Frontend: rewire task stepper/checklist/`TaskStepFields` to real API; expand 9→17 steps. *(New [api/tasks.js](frontend/src/api/tasks.js) + [useTasks.js](frontend/src/hooks/useTasks.js) hit the real endpoints; `LeadTaskTab`/`ChecklistItemRow`/`TaskStepFields` + both steppers rebuilt to the backend shape (17 steps come straight from the API, so no hardcoded count). Checklist edit = 3-state status + remark (attachments/`requires_file` dropped — Phase 8); dynamic fields render from `field_schema` with `required_when` driving conditional visibility; Save-as-Draft vs Save-&-Complete wired; per-task editing gated by the backend's `can_edit`. `getAssignableUsers` now swallows 403→`[]` so the reassign control simply hides for roles that can't read it. **The mock `useChecklist`/`api/checklist.js` are now unused by the task tab but left in place** — full `mocks/` retirement is Phase 8.)*

**Phase boundary (deferred by design, per this plan's own ordering — documented in [engine.py](backend/leads/engine.py) / [workflow_data.py](backend/leads/workflow_data.py)):**
- **Phase 5 (scheduler):** trigger tasks 2/6/11/13/15 open on a date offset. Phase 4 opens the successor **immediately** when its predecessor closes; the `trigger` hints are carried in the workflow JSON for Phase 5 to consume. Hold/unhold + `elapsed_time` also Phase 5.
- **Phase 6 (resource allocation / Project ID):** `resource_manager`- and `execution_red`-ruled tasks (2/3/4/6/7/8/9/11/12/13/14/15/16/17) open **unassigned** here (the lead owner keeps view-only access, §6; a user can be put on them via the reassign action to walk the flow). Creating `resource_allocation` rows on allocation-task open, resolving `execution_red`, and the Task-12→Hybernation / Task-17→Complete lead-status + Project-ID/`project_details` side effects are **not** applied in Phase 4 — routing still advances correctly. This matches PLAN §4's note that Project-ID/`project_details`/resource behaviour is confirmed in Phase 6.

**Per-field requiredness note:** the docs list each task's extra fields but don't mark which are mandatory for closure. Reasonable inference used in the seed: core scalar scheduling/manpower fields are `required`; "…if any" / supplementary row-groups (stakeholder contacts, invoice rows, fixed-fee blocks) are optional; Task 5's Solution-Blueprint sub-fields are `required_when` = Yes. Editable from admin if the business disagrees.

**Verification (Phase 4):** backend suite **80 tests pass** (23 new in `leads/tests.py` — workflow start/idempotency/Mining-skip, closure guards, all routing branches incl. the 3→[4,5] fork and the extension loop, draft-save global-rule rejects, complete/reassign/checklist API + permission scoping). Migration `leads.0002` applies cleanly; `seed_workflows` idempotent (17 tasks). Frontend `npm run build` + `oxlint` clean. Smoke-tested live over HTTP against `runserver` (JWT, lead-manager): create-lead-with-owner → Task 1 auto-opens (5 items, 4 fields, assigned to owner); complete-too-early 400; draft-save 200; negative-number 400; checklist-complete then Save-&-Complete → Task 1 closed + Task 2 opened. Smoke data removed afterward.

### Phase 5 — Trigger scheduler + Hold/Unhold
Implements PRD §5.6, §5.8; Tech Req §4.12, §4.9, §6. Depends on Phase 4. **Completed 2026-07-10.**
- [x] `workflow_trigger_config` model + admin; scheduled job (Celery beat or cron mgmt command) opening tasks 2/6/11/13/15 same-day when `today ≥ reference_date − offset_days`. *(`WorkflowTriggerConfig` (workflow FK / task_no / reference_task_no / reference_field_key / offset_days ≥0 / is_active, unique per (workflow, task_no)) + admin with inline offset/active editing. **Deviation — scheduler is a cron management command, not Celery:** the docs allow "Celery beat / cron mgmt command" and no broker/Celery infra exists, so the job is [open_due_tasks](backend/leads/management/commands/open_due_tasks.py) — a cron-invoked command (docstring carries the crontab line for a 06:00 daily run), mirroring the existing `seed_*` command pattern. Its logic lives in `engine.run_due_triggers(today=…)`. Seeded by a separate idempotent [seed_trigger_config](backend/leads/management/commands/seed_trigger_config.py) (run after `seed_workflows`) that reads the `trigger` hints already carried in `workflow_data.py`.)*
  - **Behaviour change vs Phase 4 (by design):** Phase 4 opened every successor **immediately** on predecessor close (a documented stopgap). Phase 5 makes trigger tasks 2/6/11/13/15 open on their date offset: `complete_task` now creates a successor **`pending`** when an active `WorkflowTriggerConfig` exists for it, and the scheduler flips it `pending → open` once `today ≥ reference_date − offset_days` (skipping held/dropped/completed leads; idempotent). *(The Phase-4 `Task.PENDING` status was reserved for exactly this.)* **The 47 Phase-4 tests still pass unchanged** because the engine test base seeds no trigger configs — with no config a successor still opens immediately (Phase-4 path). The pending/scheduler behaviour is covered by new tests that seed configs.
  - **Interpretations documented in code:** default `offset_days` seeded `0` (opens *on* the reference date — admin-tunable up) for tasks 2/6/11/15, and `60` for Task 13 (the docs' only stated offset, "2 months"); `reference_task_no`/`reference_field_key` come from the workflow JSON hints. The reference date is read from the **most-recent closed instance** of `reference_task_no` on the lead (handles the 13↔16 extension loop; noted in `engine._reference_date`).
- [x] `lead_hold` / `task_hold` models; hold/unhold endpoints (lead-hold cascades to open tasks); elapsed-time computation. *(`LeadHold`/`TaskHold` share an abstract `HoldRecord` base (hold_at/by, nullable unhold_at/by). Service layer in [holds.py](backend/leads/holds.py): `hold_lead` sets status `On Hold` + cascades a `TaskHold` to every open task; `unhold_lead` restores `In Progress` + reactivates every held task under the lead; `hold_task`/`unhold_task` for a single task; all idempotent. `elapsed_time = (closed−opened) − Σ(unhold−hold)` computed on task close in `complete_task`. Endpoints: `POST /api/leads/<id>/(un)hold/`, `POST /api/tasks/<id>/(un)hold/`, plus `GET /api/held-leads/` and `GET /api/held-tasks/` (role-scoped) for the menus. **Deviation — On Hold routed only through the hold endpoint:** the serializer now **rejects a direct `status = "On Hold"` PATCH** so the cascade + hold record can never be bypassed (Dropped/In Progress remain direct writes). **Interpretation — hold actor** (docs don't name it): lead hold = Lead Admin / lead owner / Lead-Manager creator-owner (`can_hold_lead`); task hold = assignee / lead owner / Lead Admin (`can_hold_task`, mirroring the reassign actor set).)*
- [x] Frontend: hold/unhold actions; **Held Tasks** and **Held Leads** menus. *(New sidebar entries + routes for `/held-leads` and `/held-tasks` ([HeldLeads.jsx](frontend/src/pages/HeldLeads.jsx), [HeldTasks.jsx](frontend/src/pages/HeldTasks.jsx)) backed by `api/holds.js` + `hooks/useHolds.js`. Lead detail: a Hold/Unhold button replaces the old **"On Hold" status dropdown option** (which is removed, since holding must cascade + record); the status dropdown hides while a lead is On Hold. Task stepper: a per-task Hold/Unhold button gated by the backend's new `can_hold` flag; a held task's checklist/fields are locked (existing `can_edit` already excludes non-open). `elapsed_time` + `lead_company_name`/`lead_project_name` + `can_hold` added to `TaskSerializer` for these screens.)*

**Verification (Phase 5):** backend suite **66 tests in `leads/` (99 project-wide) pass** — 19 new: trigger pending-creation, scheduler date-gating (not-due vs due, early-offset, held-lead skip, idempotency, `seed_trigger_config` idempotency), lead-hold cascade + unhold restore, hold no-op guards, held-task non-editable, `elapsed_time` net-of-holds, and the hold/unhold + held-list APIs incl. permission scoping + the direct-On-Hold rejection. Migration `leads.0003` applies cleanly; `seed_workflows`/`seed_trigger_config` idempotent (17 tasks / 5 trigger configs). Frontend `npm run build` + `oxlint` clean. Smoke-tested live over HTTP against `runserver` (JWT, lead-manager): create-lead → Task 1 open; direct `status=On Hold` PATCH 400; hold lead → status On Hold + Task 1 → hold + appears in `/api/held-leads/`; hold-again 400; unhold → In Progress + Task 1 → open; task hold → appears in `/api/held-tasks/` (with lead labels) → unhold; complete Task 1 → **Task 2 created `pending`** (trigger config present); `open_due_tasks` opens nothing while not due, then opens Task 2 once the offset makes it due. Smoke data removed afterward.

### Phase 6 — Resource Allocation, Project ID & Project Closure
Implements PRD §5.7, §5.12, §5.15; Tech Req §4.7, §4.8, §7, §9, §13. Depends on Phases 4–5. **Completed 2026-07-10.**
- [x] `resource_allocation` model (types 2HR/SNT/Implementation/Extension) auto-created when allocation tasks open; RM-only permissions; over-allocation flag; auto-close rules (2HR@T4, SNT@T9, Impl/Ext@lead-Complete). *(`ResourceAllocation` model with the 12 resource FKs (`RESOURCE_FIELDS`), `man_power_required`, `Pending→Open→Closed` status, `allocated_count`/`is_over_allocated` props. Service in [resources.py](backend/leads/resources.py): `ensure_allocation_row` inserts a `Pending` row the moment an allocation task opens (works for both immediate-open and scheduler-flipped `pending` tasks); `submit_allocation` (RM fills the form → row `Open` → allocation task closes → successor opens assigned to the chosen Execution Red); `close_allocations` auto-closes by type. Auto-close + assignment are driven by generic `on_close`/`manpower_source`/`allocation_type` hints in [workflow_data.py](backend/leads/workflow_data.py) — no task numbers hardcoded in the engine. Endpoints `/api/resource-allocations/` (list + detail PATCH + `submit/`) and `/api/allocation-users/`, all gated by the new `ResourceManagerPermission` (§12: RM-only).)*
- [x] `project_details` history + Project ID generation/regeneration on Task 12 / Task 16 (locked base + extension suffix) — Tech Req §13.1. *(`ProjectDetails` model + [projects.py](backend/leads/projects.py): `generate_first_project_id` (Task 12 — locks `project_id_base`, extension `00`, inserts the first history row linked to the Implementation allocation), `regenerate_project_id` (Task 16 — bumps the extension off the locked base, flips the prior current row → `Extended`, inserts the new current row linked to the Extension allocation), `complete_current_cycle` (Task 17). Codes read from the linked reference rows (`country.code` etc.). **Interpretation (documented in `_sequence_number`): the 3-digit sequence is a single global counter per 2-digit year** — the §13 source-sheet examples increment `…26001`→`…26002` across *different* industries in the same year, so the sequence is not per-country/industry. Admin can revisit; it's derived from the `extension_no="00"` history rows.)*
- [x] Lead status → Hybernation (T12) / Complete (T17); resource auto-close at T17. *(Applied via `engine._apply_on_close` reading the `on_close` hints: `lead_status` writes the system-only status directly on the model (bypassing the serializer guard by design — it's a system transition), `close_allocations` frees the Implementation + every Extension row at T17. **Deviation — scheduler now also processes `Hybernation` leads:** Task 12 puts the lead into Hybernation, but its Task-13 extension trigger must still fire while it sits there, so `run_due_triggers` filters `status IN (In Progress, Hybernation)` (was In Progress only in Phase 5). Held/Dropped/Complete stay excluded; the 19 Phase-5 trigger/hold tests still pass unchanged.)*
- [x] Frontend: real **Resources** screen (allocation list/edit with man-power accordion + exceeded indicator) and **Project Closure** screen (one row per `project_details` cycle, short-close control); rewire `LeadResourcesTab`. *(`api/resources.js` + `hooks/useResources.js` fully rewired to the real endpoints (the old mock resource-*request* module is gone). [Resources.jsx](frontend/src/pages/Resources.jsx): allocation reporting table (status, allocated/required man-power, red exceeded indicator) + an edit dialog whose lead/man-power context panel sits above the 12-slot form (White allows "None (TBD)"), with **Save draft** (PATCH) vs **Submit & open next task**. [ProjectClosure.jsx](frontend/src/pages/ProjectClosure.jsx): one row per `project_details` cycle (Project No, Ext, status, LM, Exec Red/Brown/White, fees) + a **Short-close** button on the current cycle → opens Task 17. New `/project-closure` route; sidebar gains **Resources** + **Project Closure**, both gated to the Resource Manager role (matching the backend RM-only permission and the §12 matrix). `LeadResourcesTab` rebuilt as a read-only per-lead allocation view. **Interpretation — "Fixed Fee Upto" (§9.2) has no dedicated workflow field, so it's surfaced as null;** Fixed Fee is summed from Task 10/14's `fixed_fee_blocks`, Variable Fee is Task 10's total cap.)*
- **Deviation — short-close vs a separate closure-open action:** §9.2's short-close "Yes" and §5-Task-17's other openers (Task 13 = No, engagement-end reached) share one code path — `engine.open_project_closure` opens the terminal closure task, found *by rule* (the task whose `on_close` sets the lead `Complete`), not by hardcoded number. The **engagement-end-date auto-open** of Task 17 is **not** built — it's not in this phase's checklist, and the normal path (Task 13 = No → routes to 17) plus short-close cover opening it; noted for a future scheduler rule.

**Verification (Phase 6):** backend suite **81 tests in `leads/` (114 project-wide) pass** — 15 new: allocation-row creation + man-power seeding, Execution-Red successor assignment, 2HR auto-close at T4, over-allocation flag, Project-ID generate/Hybernation at T12, regenerate + history cycling at T16, T17 lead-Complete + cycle-Complete + allocation auto-close, per-year sequence increment across leads, and the RM API (list/PATCH/submit, allocation-users, project-closure list, short-close + reject-twice, non-RM 403). Migration `leads.0004` applies cleanly; `seed_workflows` idempotent (17 tasks, now carrying `on_close`/`manpower_source`). Frontend `npm run build` + `oxlint` clean. Smoke-tested live over HTTP against `runserver` (JWT): walked a lead to Task 12 → `IN-PHNPD26001-I00` generated + lead Hybernation; RM `/api/resource-allocations/` showed Implementation `Open` + 2HR `Closed`; `/api/project-closure/` showed the cycle with `can_short_close` + Execution Red; short-close → 201 (Task 17 opened) then 400 on repeat; Lead Manager 403 on the allocation list. Smoke data removed afterward.

### Phase 7 — Follow-ups & Other Tasks
Implements PRD §5.11; Tech Req §4.10, §8. Depends on Phase 3. **Completed 2026-07-10.**
- [x] `followups` model + endpoints (assignee = Employee/self, no past `followup_date`). *(`Followup` model (lead FK / title / assigned_to / created_by / followup_date / remark / status `open`→`done`) + a `FollowupUpdate` comment-thread child. **Deviation — model kept wider than the docs, confirmed with the user 2026-07-10:** Tech Req §4.10's minimal shape is lead/assignee/created_by/followup_date/remark/status, but the pre-existing frontend (built against the deprecated `specs.md`) has a `title` field and a chat-style comment thread; the user chose to **keep** both, so `title` lives on `Followup` and the thread on `FollowupUpdate` on top of the docs' shape. Endpoints: `GET/POST /api/followups/` (list role-scoped + `?lead=`/`?assigned_to_me=` filters; create **Lead-Manager-only** per §12 "Add follow-up task"), `GET/PATCH /api/followups/<id>/` (retrieve + mark-done; **no delete** — closed via `status=done`), `GET/POST /api/followups/<id>/updates/` (comment thread), and `GET /api/followup-assignees/` (LM-gated). Global no-past-date rule (§3) enforced in `validate_followup_date`. **Interpretations:** (1) **visibility** — `can_view_followup` = Lead Admin sees all ("View all follow-up history", §12), everyone else sees follow-ups assigned to them **or** that they created; (2) **who may update/comment** — `can_edit_followup` = creator, assignee, or Lead Admin (docs name only the creator + assignee as participants); (3) **assignee dropdown** = all active users, since "Employee-role users" is the baseline role every user holds per CLAUDE.md. The comment endpoint uses `IsAuthenticated` + `can_edit_followup` in `perform_create` (not `FollowupPermission`, whose LM-only POST rule applies to raising a follow-up, not commenting on one).)*
- [x] Frontend: rewire follow-up create/list/update; **Other Tasks** screen filtered to the logged-in user. *(`api/followups.js` + `hooks/useFollowups.js` fully rewired to the real endpoints — the old mock (localStorage `followups`/`followupUpdates`) is gone. New top-level **Other Tasks** screen ([OtherTasks.jsx](frontend/src/pages/OtherTasks.jsx), `/other-tasks`, sidebar entry) lists the caller's own follow-ups (`?assigned_to_me`) with an LM-only "New follow-up" button carrying a lead dropdown (§8's standalone add action). `LeadFollowUpsTab` rebuilt to the backend shape (per-lead list + create, lead implicit); `FollowupUpdateDialog` rewired to the real comment-thread endpoint + `open`/`done` status; a shared `FollowupCreateDialog` backs both create surfaces (title/date/assignee/remark, date floored to today). `PERMISSIONS.manageFollowups` tightened to Lead-Manager-only to mirror the backend. **Deviation — removed the old `/leads/follow-ups` sub-route + `LeadsLayout` tab and deleted `FollowUpsList.jsx`/`LeadsLayout.jsx`:** the cross-lead follow-ups list under Leads is superseded by the docs' top-level "Other Tasks" screen; `/leads` now mounts `LeadsList` directly.)*

**Verification (Phase 7):** backend suite **97 tests in `leads/` (130 project-wide) pass** — 16 new in `leads/tests.py` (create rules + LM-only gating, status-forced-open, past-date reject, visibility scoping incl. assignee/creator/Lead-Admin, mark-done + out-of-scope 404, no-delete 405, comment thread add/list + non-participant block, assignee-list gating). Migration `leads.0005` applies cleanly. Frontend `npm run build` + `oxlint` clean. Smoke-tested live over HTTP against `runserver` (JWT): employee create 403; LM past-date 400; LM create 201 (open) → employee sees it in Other Tasks; employee comment 201; LM thread list = 1; employee mark-done 200; assignee list LM 200 / employee 403; delete 405. Smoke data removed afterward.

### Phase 8 — Dashboard, Activity, Notifications, Attachments & mock retirement
Implements PRD §6 (dashboards, activity log) + Decision #4 (keep Activity/Notifications/Attachments). Depends on all prior. **Completed 2026-07-11.**
- [x] Leads-funnel dashboard aggregation endpoint (own vs all, per role). *(`GET /api/dashboard/` (`DashboardView`) reuses `LeadQuerysetMixin`, so the funnel is scoped by lead visibility — a Lead Admin's spans every lead, a Lead Manager's/Marketing's their own (the §6 "own vs all leads-funnel" rows). Returns `count_by_status` (all 5 statuses), `active_leads` with a closed/total-task `progress` %, `overdue_followups` (always the caller's own), and total/active/completed/dropped counts. **Interpretation:** the §6 matrix grants the funnel only to LM/LA; users with no lead scope (Employee/RM) get an empty funnel + their own overdue follow-ups rather than a 403, since `/dashboard` is the shared landing page.)*
- [x] Activity-log model/endpoint (auto-logged events) — wire the existing Activity tab. *(`ActivityLog` (lead FK / actor / type / summary / body) + `GET /api/leads/<id>/activities/`, scoped through `LeadQuerysetMixin` so activity visibility inherits lead visibility (the §6 "own vs all activity log" rows). **Deviation — logged at the API view boundary, not inside the engine:** events are recorded in the views where `request.user` is available (lead created, owner assigned, status changed, task completed + each successor opened, task reassigned, lead/task hold+unhold, resources allocated, follow-up raised), via a small `events.log_activity` helper — deliberately leaving the heavily-tested workflow engine untouched. Auto-logged + best-effort per NFR §7.)*
- [x] Attachments model + upload/download endpoint (5MB cap) — wire the existing Files tab. *(`Attachment` (lead FK / `FileField` / filename / title / uploaded_by) + `GET/POST /api/leads/<id>/attachments/` (multipart) and `DELETE /api/attachments/<id>/`. 5 MB cap enforced in `AttachmentSerializer.validate_file`; download is the served `MEDIA` URL (`build_absolute_uri`). Added `MEDIA_URL`/`MEDIA_ROOT` + dev static-serving in `config/urls.py`. **Deviation — lead-scoped, not a generic entity table:** the only upload surface is a lead, so the model is lead-scoped (the frontend's `entity_type='lead'` arg is now vestigial). **Permissions:** view/upload = anyone who can see the lead; delete = the lead's owner/creator or a Lead Admin (`can_hold_lead`), enforced in `perform_destroy` (the delete view uses `IsAuthenticated` + the scoped queryset, since `LeadPermission`'s object check expects a Lead).)*
- [x] In-app Notifications model + endpoints — wire the existing bell/notifications page. *(`Notification` (user FK / type / message / link / is_read) + `GET /api/notifications/`, `POST /api/notifications/<id>/read/`, `POST /api/notifications/mark-all-read/`. Auto-generated via `events.notify` on owner assignment (→ new owner), task open (→ assignee, incl. via the RM allocation-submit path), task reassign (→ target), and follow-up raise (→ assignee). **Note on PRD §8:** the PRD lists notifications as deferred/out-of-scope, but PLAN Decision #4 (which governs) keeps them in this build — followed the plan and built the in-app flow; email notifications remain future scope. The serializer exposes `read` (aliasing `is_read`) so the existing bell/page read unchanged.)*
- [x] Backend forgot-password endpoint (replace the mocked flow); optional `/me` endpoint. *(`PasswordResetToken` (UUID handle, 30-min TTL, single-use) + `POST /api/auth/password-reset/` (request), `GET`/`POST /api/auth/password-reset/<uuid>/` (verify / confirm). `GET /api/auth/me/` (`MeView`) returns the caller's own `UserSerializer` payload. **Deviation — no email backend:** the request endpoint always responds 200 (no email enumeration) and, **in DEBUG only**, returns the reset link in the body so the flow is usable in dev — a real deployment would email it (documented in the view).)*
- [x] Frontend: rewire dashboard/activity/notifications/attachments to real API; **delete `mocks/` and `scope.js`'s mock reads**; remove the dead `RoleSwitcher`. *(`api/{dashboard,activities,attachments,notifications}.js` + their hooks now hit the live endpoints (attachments upload via `FormData`; activity is read-only since it's auto-logged, so `useLogActivity` is dropped); `Dashboard.jsx` rebuilt to the backend shape (dropping `useCompanies`); `api/auth.js` forgot-password rewired + `getMe` added; `AuthContext` now confirms identity via `/me` on load. `ForgotPassword`/`ResetPassword` adjusted to the new response shapes. **Mock retirement:** deleted the whole `mocks/` dir (`db.js`, `seed.js`), the now-dead `api/{companies,leadTypes,checklist}.js` + `hooks/use{Companies,LeadTypes,Checklist}.js`, `CompanyCombobox.jsx`, and `RoleSwitcher.jsx` (+ its `AuthContext.switchUser` no-op); `scope.js` stripped of `peek`/`visibleLeadIds`/`canViewLead` (now pure UI-gating role/permission helpers). No `mocks/` references remain in the codebase.)*

**Verification (Phase 8):** backend suite **151 tests project-wide pass** (21 new: activity auto-log + lead-scoped visibility, attachment upload/list/oversize-reject/delete-permission, notification auto-generate + list/mark-read/mark-all scoping, dashboard own-vs-all funnel + empty-funnel-with-own-follow-ups, `/me`, and the full password-reset request→verify→confirm→reuse-reject flow incl. DEBUG-link). Migrations `authentication.0004` + `leads.0006` apply cleanly. Frontend `npm run build` + `oxlint` clean (only pre-existing fast-refresh warnings). Smoke-tested live over HTTP against `runserver` (DEBUG): `/me` ✓; dashboard own=1 vs Lead-Admin all=2 ✓; create-lead → activity log (`Lead created` + `Lead assigned`) + owner `lead_assigned` notification ✓; attachment upload (served URL 200) + >5 MB → 400 ✓; reassign Task 1 → target `task_reassigned` notification ✓; forgot-password verify/confirm 204 + reused-token 400 ✓. Smoke lead/attachment/notifications/tokens removed afterward. *(The seeded dev accounts `tarun`/`rahul`/`varun`/`shailesh` had their passwords set to a known value for the HTTP smoke — dev-only data, original hashes were unknown.)*

**Phase 8 completes the plan** — all lead-management domain screens now run against the live backend; nothing remains on the retired mock layer.

### Phase 9 — Post-launch bug fixes & permission refinements

Reported by the user after Phase 8 shipped; not spec'd in the PRD/Tech Req, so decisions below were confirmed directly with the user rather than derived from the docs.

- [x] **Lead ID.** No such concept exists in either doc — net new. Added a read-only `lead_display_id` (`LD-{created_at.year}-{id:05d}`) to `LeadSerializer`, shown in `LeadsList`'s "Lead ID" column (replacing the raw `#{id}`) and as its own stat card on `LeadDetail`, visually distinct from the Project-ID format (`IN-PHNPD26001-I00`). The "Project ID" stat card is now hidden entirely (not shown with a "Pending" placeholder) until `lead.project_id` is actually generated at Task 12.
- [x] **Task edit/reassign permissions.** `can_edit_task`/`can_reassign_task` (`backend/leads/permissions.py`) were assignee-only; the lead's owner (`assigned_to`) and creator (`created_by`) couldn't edit or reassign a task on their own lead, which is also why reassign was silently 403'ing from the UI (the button rendered regardless of permission). Both functions now share one rule: assignee, lead `assigned_to`, lead `created_by`, or Lead Admin, while the task is open. `TaskSerializer` gained a `can_reassign` flag so the Reassign button only renders when the caller is actually allowed.
- [x] **Save & Complete bug.** `TaskStepFields` only persisted typed field values on its own "Save as Draft" click; "Save & Complete" (`LeadTaskTab`) called the bodiless `complete` endpoint directly, validating against whatever was last drafted — so typing values and completing without drafting first produced a false "field is missing" error. Fixed by having `LeadTaskTab` trigger `TaskStepFields`' draft-save (via an imperative handle) immediately before calling complete.
- [x] **Owner vs. assigned-to terminology.** Per user clarification, "owner" now means the lead's **creator** (`created_by`) — already immutable, never reassignable. "Assigned to" is the BD/LM person actively working the lead (`assigned_to`) and remains reassignable. The UI previously labeled `assigned_to` as "(owner)", conflating the two; relabeled throughout `LeadForm`/`LeadDetail` to drop that conflation, and the "Created by" card is now labeled "Owner". No backend field rename — this is a display-layer fix only, decided with the user rather than reopening the data model.
- [x] **Assignable-to pool.** `AssignableUserListView` was hard-restricted to the `lead_manager` group. Broadened to any active user **except** Marketing and Finance group members, per the user's direction (a belts-based eligibility rule is explicitly future scope, not built here).
- [x] **Checklist sequencing.** Verified, no change needed: checklist items already have no ordering lock (any item can be updated in any order); a task only closes — opening the next one — once every item is `complete` and mandatory fields are filled. This already matches the requested behavior.
- [x] **Follow-up tab for Resource Manager.** `canSeeLeadModule` excluded Resource Manager (bucketed as "back office") from the `/other-tasks` nav item. Added a narrower `canSeeFollowUps` predicate (Lead-module roles, or no back-office role, or Resource Manager specifically) so only the Follow-up nav item opens up for Resource Manager — Leads/Held Leads/Held Tasks stay hidden, per the user's choice.

### Phase 10 — Post-Phase-9 bug fixes & Resource-module rework

Second round of user-reported issues (2026-07-13). Not spec'd in the PRD/Tech Req; product decisions confirmed directly with the user (recorded per item). Some Phase-9 backend work landed but never reached the UI (the frontend lead adapter drops the new fields) — those are finished here.

**10a. Display/adapter bug fixes (frontend-only)**
- [x] **Lead ID not visible.** Backend already returns `lead_display_id` (Phase 9) but `fromApiLead` ([frontend/src/api/leads.js:29-55](frontend/src/api/leads.js#L29-L55)) never copies it, so `LeadsList`/`LeadDetail` render blank. Add `lead_display_id` (and `progress`, same omission) to `fromApiLead`.
- [x] **"Overall task progress, not checklist progress."** Backend `progress` = closed tasks / total tasks (this *is* overall task progress). Two fixes: (1) pass `progress` through the adapter (10a above); (2) relabel the `LeadDetail` stat card from "Checklist completion" to "Task progress" ([LeadDetail.jsx](frontend/src/pages/LeadDetail.jsx)) so the number matches its label. Confirm `ProgressRing`/`ProgressBar` render the value.

**10b. Session leakage on logout/login (auth hardening)**
- [x] **Clear React Query cache on logout.** `AuthContext.logout` only nulls the user; the module-level `queryClient` singleton keeps the previous user's cached leads/tasks/etc., so the next login flashes stale data. Call `queryClient.clear()` in `logout` (and defensively on successful login).
- [x] **Role-guard routes, not just the sidebar.** `RequireAuth` already accepts a `roles` prop but `App.jsx` never passes it, so any authenticated user can hit `/resources`, `/users`, `/finance`, `/leads`, etc. by URL. Wire `roles` on the role-specific routes (Resource Manager → `/resources`, `/project-closure`; User Management → `/users`; Finance → `/finance`; lead-module → `/leads`, `/held-*`, `/other-tasks` via `canSeeLeadModule`/`canSeeFollowUps`), redirecting unauthorized users to `/dashboard`.

**10c. Save-as-draft on task "Additional details" (reproduce → fix)**
- [x] Reproduce the "fill partially → Save as Draft → refresh → gone" report against the live backend. The draft PATCH *does* persist `extra_fields`; prime suspect is `buildPayload` ([TaskStepFields.jsx:150-163](frontend/src/components/leads/TaskStepFields.jsx#L150-L163)) sending only currently-*visible*, non-empty fields while the PATCH replaces the whole blob — a hidden/cleared field is dropped. Fix so a partial draft round-trips intact.

**10d. Resource module rework** (PRD §5.7 / Tech Req §4.7, §7, §9)
- [x] **Preserve the Brown/White manpower split** *(decision: keep numbers, show the split)*. `_manpower_required` currently sums Brown+White into one `man_power_required`. Add `man_power_brown` / `man_power_white` on `ResourceAllocation`, seed both in `ensure_allocation_row`, keep the total for the existing over-allocation check. Migration + serializer fields.
- [x] **Multi-select Browns and White** *(decision: two multi-selects, rest single)*. Convert `execution_brown` and `white` from single FK → M2M (e.g. `execution_browns`, `whites`); Execution Red, Auditor 1–4, Project Member 1–5 stay single FKs. Update `allocated_count` (single slots filled + M2M members), make `is_over_allocated` type-aware (allocated Browns vs required Brown, Whites vs White), update `ResourceAllocationSerializer` (M2M read/write + `resource_names`), and the `ProjectDetails`/Project-Closure display that reads Exec-Brown/White names. Data migration: existing single FK value → one M2M member. `submit_allocation` still assigns the next task to the single `execution_red` — unchanged.
- [x] **Single CTA on the allocation form** *(user: only "Submit / Allocate resource")*. In `AllocationDialog` ([Resources.jsx:134-146](frontend/src/pages/Resources.jsx#L134-L146)) remove "Save draft" and "Submit & open next task"; one primary button — "Allocate resources" on a `Pending` row (saves + submits, closing the allocation task and opening the next per spec), "Update allocation" on an `Open` row (save only), read-only when `Closed`.
- [x] **Project details for RM clarity** (PRD §5.7 "accordion with the lead's relevant details"). Enrich the allocation context panel with the lead/project detail an RM needs to staff confidently: Lead ID, company, project, country/industry/domain, division/scope, Lead Manager, allocation type, and the Brown/White manpower breakdown (required vs allocated per type) with per-type over-allocation flags.
- [x] **Verify + surface auto-free** *(decision: keep spec auto-free, no new status)*. Confirm end-to-end that `close_allocations` fires (2HR → Task 4, SNT → Task 9, Implementation/Extension → lead Complete at Task 17) and flips rows to `Closed`. On the Resource screen make the lifecycle legible: `Open` = "tied up", `Closed` = "freed" (label/tooltip), so it's clear at a glance which resources are still allocated vs released.
- [x] **Resource-role nav** — already matches the requested Dashboard / Resources / Project Closure / Follow up / Notifications set (Phase 9 `canSeeLeadModule`/`canSeeFollowUps`); verify only, no change expected.

**10e. Verification (done)** — **backend suite 155 tests pass** (new: manpower-split seeding, multi-select PATCH round-trip, per-belt over/under-allocation, SNT@9 auto-close; save-as-draft round-trip proven by a throwaway test). Migration `leads.0007` applies cleanly (data-preserving FK→M2M) + seeds idempotent; frontend `npm run build` + `oxlint` clean. **Live HTTP smoke over `runserver`:** Lead ID (`LD-2026-00022`) + progress (50) on the lead payload; RM lists allocation with full lead context + Brown/White split (2/1); multi-select PATCH round-trips (allocated 4, not over at exact counts); over-allocation flips `True` on a 3rd Brown; non-RM 403 on `/resource-allocations/`. Smoke data removed afterward. *(Route-guard redirect, logout cache-clear, and the multi-select popover are client-only behaviours verified by build/lint + code, not the HTTP smoke.)*

### Phase 11 — Post-Phase-10 permission rework & UX fixes

Third round of user-reported issues (2026-07-14). Not spec'd in the PRD/Tech Req; product decisions confirmed directly with the user (recorded per item). The three genuine conflicts/ambiguities were resolved with the user before planning:
- **Hold TASK → assignee only** (points #3 and #5 conflicted; #5 wins). A Lead Manager gets Hold Task only when self-assigned (they *are* the task assignee).
- **Hold/reassign LEAD → Lead Manager (+ Lead Admin)**; the plain-employee assignee can no longer hold the lead, only their own task.
- **Expected start date floor → the lead's creation date** — a documented per-field exemption from the global "no past dates" rule (CLAUDE.md allows PRD-exempt fields; this is one).

**11a. Permission rework (backend + frontend gating)** — [permissions.py](backend/leads/permissions.py), [serializers.py](backend/leads/serializers.py) (`can_hold`/`can_edit`/`can_reassign` flags), `PERMISSIONS` in [frontend/src/api/scope.js](frontend/src/api/scope.js), `LeadDetail.jsx`, `LeadTaskTab.jsx`. **Done.** *(Decision confirmed with the user: Lead Admin is retained as an administrative override on task edit/hold; everyone else is strictly the assignee. Side effect: attachment-delete reuses `can_hold_lead`, so a plain-employee assignee no longer deletes lead files — consistent with "lead management = LM/LA", `scope.js` mirror updated to match.)*
- [x] **Only the assignee edits a task** (#5). `can_edit_task` ([permissions.py:~90](backend/leads/permissions.py)) is currently assignee ∪ lead `assigned_to` ∪ lead `created_by` ∪ Lead Admin (broadened in Phase 9). Narrow to **the task's `assigned_to` only** — the lead's creator (LM *or* Marketing) can no longer complete checklists/fields just by having created/owned the lead. *Decision to confirm: keep Lead Admin as an administrative override (consistent with Lead Admin being the lead super-role everywhere else) — flagged, not assumed.*
- [x] **Hold TASK → assignee only.** `can_hold_task` ([permissions.py:130-140](backend/leads/permissions.py#L130-L140)) is currently assignee ∪ lead owner ∪ Lead Admin. Narrow to **the task assignee only**. `TaskSerializer.get_can_hold` ([serializers.py:278-282](backend/leads/serializers.py#L278-L282)) then drives the `LeadTaskTab` Hold button so a plain employee assignee sees Hold Task and a non-assignee LM does not (self-assigned LM still sees it).
- [x] **Hold/reassign LEAD → Lead Manager (+ Lead Admin).** `can_hold_lead` ([permissions.py:113-127](backend/leads/permissions.py#L113-L127)) currently includes the bare `assigned_to==user` branch (any assignee, incl. plain employees). Remove that branch; keep **Lead Admin ∪ (lead_manager group AND (created_by==user OR assigned_to==user))** so an LM who created *or* was assigned the lead can hold it, self-assigned LM works, but a plain-employee assignee cannot. Mirror in `PERMISSIONS.holdLead` (scope.js) for the `LeadDetail` Hold-Lead button. Confirm lead **reassignment** (changing `assigned_to` on an existing lead via the `LeadDetail` dialog / `LeadDetailView.perform_update`) is already gated to LM/LA (`CanAssignOwnerPermission`) and stays that way.

**11b. Activity log: actor stamp + remarks + reassignment (#1)** — [events.py](backend/leads/events.py), [views.py](backend/leads/views.py), `ActivityLogSerializer` ([serializers.py:619-636](backend/leads/serializers.py#L619-L636)), `LeadDetail.jsx` Activity tab (lines 201-221).
- [x] **Render the acting user.** `actor_name` is already serialized but not shown — render it on every activity card ("— by {actor_name}") in the Activity tab.
- [x] **Render remarks.** `ActivityLog.body` is already displayed when present; wire the write side so user-supplied remarks reach it. Add an optional **remark** field to the Hold-Lead / Hold-Task / reassign actions (frontend input + pass through the hold/reassign endpoints → `log_activity(..., body=remark)`). Where no remark is added, nothing extra shows ("if he added it").
- [x] **Log lead reassignment.** Verify/complete: changing a lead's `assigned_to` on an already-assigned lead logs a "Lead reassigned from X to Y" activity in `LeadDetailView.perform_update` (today only the unassigned→assigned path logs via `_notify_owner_assigned`, [views.py:105-125,174-184](backend/leads/views.py#L105-L125)). Task reassignment already logs ([views.py:381](backend/leads/views.py#L381)).

**11c. Lead-detail display tweaks (frontend-only)** — `LeadDetail.jsx`, `LeadForm.jsx`.
- [x] **Remove the Created-date card** ([LeadDetail.jsx:181-184](frontend/src/pages/LeadDetail.jsx#L181-L184)).
- [x] **Rename the progress card** "Task progress" → **"Overall progress"** ([LeadDetail.jsx:156-162](frontend/src/pages/LeadDetail.jsx#L156-L162)); value (`lead.progress`, closed/total tasks) unchanged.
- [x] **Self label on assignment.** In the `LeadForm` assignable-owner dropdown ([LeadForm.jsx:195-206](frontend/src/pages/LeadForm.jsx#L195-L206)), when an option's user id === the logged-in user id, render it as "**{name} (self)**". (Also apply the "(self)" suffix on the `LeadDetail` "Assigned to" card when the viewer is the assignee.)

**11d. Expected-start-date floor = lead creation date (#4)** — [engine.py:167-189](backend/leads/engine.py#L167-L189), `TaskStepFields.jsx`.
- [x] **Backend:** for the `date` scalar validator, replace the hard "before today" reject with "before the **lead's `created_at` date**" for this field. Scope carefully — the global "no past dates" rule stays for all *other* date fields; this is a per-field exemption. Simplest: pass the lead's creation date as the floor for `expected_start_date` specifically (the field carries `key == "expected_start_date"`), keep `today` as the floor otherwise. Document as a Phase-11 exemption.
- [x] **Frontend:** set the date `<Input>`'s `min` attribute ([TaskStepFields.jsx:18-42](frontend/src/components/leads/TaskStepFields.jsx#L18-L42)) to the lead's creation date for `expected_start_date` (and `today` for other date fields) so the picker enforces it client-side too.

**11e. Surface allocated manpower on the lead's Resources tab (#6)** — new lead-scoped read endpoint + [LeadResourcesTab.jsx](frontend/src/components/leads/LeadResourcesTab.jsx).
- [x] **Backend:** `LeadResourcesTab` currently reads the RM-only `/resource-allocations/` list, which returns `[]` for the lead's own people. Add a **lead-scoped read** — e.g. `GET /api/leads/<id>/resource-allocations/` gated by lead visibility (assignee/creator/LM/Lead Admin), returning the lead's `ResourceAllocation` rows via the existing `ResourceAllocationSerializer` (type, status, `resource_names`, Brown/White counts, `man_power_required`/`man_power_brown`/`man_power_white`).
- [x] **Frontend:** point `LeadResourcesTab` at the new endpoint and render the allocated resources + Brown/White manpower split (read-only) so the lead's people can see which manpower was allocated. (Editing stays RM-only on the Resources screen.)

**11f. Belt tag outlines in User Management (frontend-only)** — `BeltBadge` in [StatusBadge.jsx:76-129](frontend/src/components/shared/StatusBadge.jsx#L76-L129).
- [x] Today `BELT_POTENTIAL_STYLES` renders every "Potential …" belt with one flat grey dashed style. Change so each **Potential** belt gets a **dashed outline in its base colour** (Potential Red → red outline, Potential Brown → brown outline, Potential Black → black outline) — coloured border + tinted text, transparent fill to keep it visually "not yet earned" vs the solid defined belts. **Except Potential White → a grey dashed outline with a hint of white** (white has no visible outline of its own). Defined belts (Black/Brown/Red/White) and `NA` unchanged.

**11g. Resource-allocation dialog: click-outside dismiss bug (Resource module #1)** — `AllocationDialog` / `MultiUserSelect` in [Resources.jsx](frontend/src/pages/Resources.jsx), possibly [ui/dialog.jsx](frontend/src/components/ui/dialog.jsx).
- [x] The Browns/White multi-select is a Radix **Popover rendered in a Portal** (outside the Dialog's DOM node), so clicking inside the open dropdown registers as "outside" the Dialog and Radix's default dismiss closes the **whole** `AllocationDialog`. Fix by passing `onInteractOutside`/`onPointerDownOutside` to the dialog's content that **ignores events originating from the popover** (target within `[data-radix-popper-content-wrapper]`), so clicking outside the dropdown only closes the dropdown, and only a click truly outside the dialog closes the dialog. Prefer scoping the fix to `AllocationDialog` (pass the handler as a prop) rather than changing the shared `dialog.jsx` globally, unless the shared component doesn't forward the prop — decide during implementation.

**11h. Verification (done)** — **backend suite 171 tests pass** (+8 new in `leads/tests.py`: task edit assignee-only incl. self-assigned-LM 200 / non-assignee-LM 403, task-hold assignee-only + Lead-Admin-override notify, plain-employee-cannot-hold-lead, expected_start_date floor = lead-creation accepts back-date but rejects earlier, lead-reassignment activity logged with actor + from→to + remark, lead-scoped resource-allocation read 200 for the lead's people / 404 out of scope). **No new migrations** (`makemigrations --check` clean — Phase 11 is permissions/serializers/views/UI only). Frontend `npm run build` + `oxlint` clean (only pre-existing fast-refresh warnings). Client-only behaviours (belt outlines, "(self)" label, activity actor/remark render, Hold-with-remark dialogs, allocation-dialog popover fix) verified by build/lint + code, matching how Phase 10 recorded client-only changes.

**Decision recorded:** Lead Admin retained as an administrative override on task edit/hold (the user approved this carried decision); attachment-delete reuses `can_hold_lead`, so a plain-employee assignee no longer deletes lead files (consistent with "lead management = LM/LA"; the `scope.js` `deleteAttachment` mirror was updated to match).

### Phase 12 — Resource single-Brown, red-mandatory, repeat-assignee hints & lead access for task workers

Fourth round of user-reported changes (2026-07-15). Not spec'd in the PRD/Tech Req; the two scope decisions were confirmed with the user before planning:
- **Follow-up creation opens to any lead-viewer, everywhere** — anyone who can see a lead (LM, lead owner, an assigned Red/any task assignee, and the Resource Manager) may raise follow-ups on the lead's Follow-up tab *and* the standalone Other Tasks screen (replacing the Lead-Manager-only rule).
- **Resource Manager gets view-only lead access; allocation stays on the `/resources` screen** (linked from the lead), not embedded in the lead page.

**12a. Execution Brown → single-select (White stays multi); Brown count capped at 1** — [models.py](backend/leads/models.py), [serializers.py](backend/leads/serializers.py), [workflow_data.py](backend/leads/workflow_data.py), [engine.py](backend/leads/engine.py), [Resources.jsx](frontend/src/pages/Resources.jsx), [LeadResourcesTab.jsx](frontend/src/components/leads/LeadResourcesTab.jsx), [TaskStepFields.jsx](frontend/src/components/leads/TaskStepFields.jsx).
- [x] **Model:** revert `execution_browns` (M2M, Phase 10d) → a single `execution_brown` FK; `whites` (M2M) stays. Move `execution_brown` into `SINGLE_RESOURCE_FIELDS`; `MULTI_RESOURCE_FIELDS` becomes `["whites"]`. Update `allocated_count` (single slots incl. brown + white M2M members), `brown_count` (0/1), and `is_over_allocated` (brown_count > man_power_brown OR white_count > man_power_white). Migration `leads.0009` — data-preserving M2M→FK (first Brown in the set becomes the FK).
- [x] **Brown count cap = 1:** the `manpower_brown` number fields (tasks 1/5/10/14) get a `"max": 1` in the schema; add `max` support to the engine's number validator (`_validate_scalar`) and the frontend number input (`ScalarInput` gets a `max`). (White manpower stays uncapped.)
- [x] **Serializers:** `ResourceAllocationSerializer` — drop `execution_browns` writable M2M, add `execution_brown` single FK (writable); `resource_names.execution_brown` becomes a single label. `ProjectDetailsSerializer.execution_browns` → `execution_brown` single. Update `ResourceAllocation.RESOURCE_FIELDS` usages, prefetch/`select_related` (browns no longer prefetched; brown joins `select_related`).
- [x] **Frontend:** Resources.jsx — Brown becomes a single `UserSelect` (like Execution Red), White stays `MultiUserSelect`; over-allocation line reads brown 0/1. LeadResourcesTab + Project-Closure display read the single Brown name.

**12b. Execution Red mandatory on allocation** — [resources.py](backend/leads/resources.py) `submit_allocation`, [Resources.jsx](frontend/src/pages/Resources.jsx).
- [x] Every allocation step (2HR/SNT/Implementation/Extension) hands its successor to the chosen Execution Red, so **Red is required to submit**. `submit_allocation` raises a `ValidationError` if `execution_red` is unset. Frontend: mark Execution Red with `*` and disable the "Allocate resources" button until a Red is chosen (the save-only "Update allocation" on an already-Open row is unaffected).

**12c. "Previously assigned" hint on repeating allocation steps** — [Resources.jsx](frontend/src/pages/Resources.jsx) (data already available).
- [x] In `AllocationDialog`, fetch the lead's *other* allocation rows (`/api/resource-allocations/?lead=<id>`), collect the users assigned in earlier rows, and annotate each dropdown option's name with the prior allocation type(s) they appeared in — e.g. "John Doe (prev: 2HR)". Not shown on the first allocation (no earlier rows). Helps the RM re-pick the same people across steps without memorising names.

**12d. Auto-advance the task stepper to the next step on completion** — [LeadTaskTab.jsx](frontend/src/components/leads/LeadTaskTab.jsx).
- [x] `handleComplete` already jumps to `opened_tasks[0]`; make it robust — if nothing opened (or the successor is a trigger-`pending` task), advance to the next task in sequence so completing a step always lands the user on the corresponding next step. Verify against immediate-open and pending-trigger paths.

**12e. Lead visibility + reachable lead page for task workers and the RM** — [views.py](backend/leads/views.py) `LeadQuerysetMixin`, [App.jsx](frontend/src/App.jsx), `scope.js`, [LeadDetail.jsx](frontend/src/pages/LeadDetail.jsx).
- [x] **Backend visibility:** broaden `LeadQuerysetMixin` so (a) **any user assigned to a task on the lead** sees it (`Q(tasks__assigned_to=user)`) — this is what lets the assigned Red open the lead page to work their step; and (b) a **Resource Manager** sees leads that have a `resource_allocation` row (`Q(resource_allocations__isnull=False)` when RM). `.distinct()` already applied. Writes stay blocked by `LeadPermission` (view-only for these users); the Red still edits *their own task* via `can_edit_task` (assignee) — "view-only" applies to the lead + others' steps, not the worker's own task.
- [x] **Route/UI:** loosen the `/leads/:id` route guard so RM (and any task worker) can open a lead detail they're entitled to (backend still scopes visibility); keep the Leads *list* (`/leads`) gated by `canSeeLeadModule`. Add a graceful not-authorised/not-found state to `LeadDetail` for a lead the caller can't see (avoid the infinite "Loading…"). Link each Resources-screen allocation row to its lead so the RM can jump to the (view-only) lead page.

**12f. Follow-up creation for any lead-viewer** — [permissions.py](backend/leads/permissions.py), [views.py](backend/leads/views.py), [serializers.py](backend/leads/serializers.py), `scope.js`, follow-up create dialogs.
- [x] **Backend:** `FollowupPermission` POST → any authenticated user; the follow-up serializer/view validates the target `lead` is one the caller can **view** (reusing the lead visibility scope), so a user can only raise a follow-up on a lead they can see. `FollowupAssigneeListView` (`CanAddFollowupPermission`, LM-only) → open to any authenticated user so non-LM creators can load the assignee list. Activity/notify wiring unchanged.
- [x] **Frontend:** `PERMISSIONS.manageFollowups` broadens from LM-only to any lead-viewer, so the "New follow-up" button shows on both the lead Follow-up tab and Other Tasks; the standalone create dialog's lead dropdown lists the caller's visible leads.

**12g. Verification (done)** — **backend suite 180 tests pass** (new `Phase12AccessTests`: successor assigned to chosen Red, Red can retrieve the lead + edit only their own task + is 403 on lead edits + can create a follow-up, non-participant 404s, RM sees the lead once it has an allocation / 404 without one, Execution-Red-required blocks submit then unblocks; plus single-Brown/multi-White PATCH round-trip, White-over-allocation flag, Brown-manpower-capped-at-1 reject, follow-up creation gated by lead visibility incl. task-worker/Lead-Admin, assignee-list open to any authenticated). Existing walk/branch tests updated for Brown-single + the manpower-cap fixtures. Migration `leads.0009` applies cleanly (data-preserving M2M→FK) and `seed_workflows` re-seeded (17 tasks, Brown fields now carry `max:1`). Frontend `npm run build` + `oxlint` clean (only pre-existing fast-refresh warnings). Client-only pieces (single-Brown allocation form, Red-required disable, "(prev: …)" hint, auto-advance ref, RM/Red lead-page access + not-found state) verified by build/lint + code.

**Note — the previously-uncommitted `leads.0008_alter_notification_type` migration was applied to the dev DB during this phase** (it had been generated but not yet run); it applied cleanly ahead of `0009`.

### Phase 13 — Post-Phase-12 flow correction, hold visibility & UX fixes

Fifth round of user-reported changes (2026-07-15). The one item that overrides the spec was confirmed with the user before planning; the rest are bug-fixes/enhancements.

**Confirmed spec override (item 13a):** After allocation, the **allocated Execution Brown + White(s) edit** the downstream execution tasks; **Execution Red is view-only across all steps** (as are the BD owner / Lead Manager). This reverses PRD §5.7 / the workflow table ("Edit: execution red") — the `*_updated.md` docs are updated to match, with a dated note.

**13a. Invert task-edit model — Brown+Whites edit, Red view-only.**
- [x] `workflow_data.py`: execution tasks (3,4,7,8,9,12,13,14,16,17) `assignee` `execution_red` → `execution_brown`; reseed via `seed_workflows`.
- [x] `resources.py`: `latest_task_assignee(lead)` (current allocation's Brown, else first White); `current_allocation_editor_ids(lead)` (Brown + Whites of the current non-closed row); `lead_allocation_people_ids(lead)` (every person on any allocation row — for Red/auditors view); `user_allocation_q(user, prefix)` for queryset scoping.
- [x] `engine.py` `_resolve_assignee`: `execution_brown` → `resources.latest_task_assignee`.
- [x] `permissions.py`: `can_edit_task` — assignee (Brown/first-White) **plus** any White of the current allocation may edit an execution task; `can_view_task` — anyone on any of the lead's allocation rows (incl. Red) gets view-only. `views._scoped_tasks` / `lead_scope_q` broadened with `user_allocation_q` so allocated people (Red included) can list/open the tasks.
- [x] `resources.submit_allocation`: require an editor (Brown or ≥1 White) in addition to the existing Execution-Red requirement (Red stays the mandatory overseer).
- [x] Docs: update `LMS_PRD_updated.md` §5.7 + workflow table + §6/§12 and the Technical-Requirements tables with the dated override note.

**13b. Step 13 not editable with offset 0.** Fixed by 13a (Brown/White now edit). Also documented the offset mechanic: a trigger task opens on `today ≥ reference_date − offset_days`, so offset 0 opens it on the reference date, not immediately — a task still `pending` (reference date in the future) is uneditable by design. Verify `latest_task_assignee` resolves off the Implementation row (stays Open post-Task-12).

**13c. Resource table matches the Lead table.** `Resources.jsx`: rows `cursor-pointer` + row-click opens the allocation dialog; keep the Edit/View icon button; `stopPropagation` on the inner Project link + Edit button. Over-allocation indicator retained.

**13d. Allocation-dialog dropdown click-outside no longer dismisses the dialog.** `Resources.jsx`: give `UserSelect`'s `SelectContent` `position="popper"` and broaden `isInPortalLayer` to also match Radix Select content (`[data-radix-select-content]`/viewport) — item-aligned Select content carried no popper wrapper, so its outside-click reached the Dialog and closed it ("2 steps out").

**13e. Scheduled-open date for pending trigger tasks.** Backend: `TaskSerializer` gains `scheduled_open_date` / `opens_in_days` / a human hint for `pending` tasks (from `WorkflowTriggerConfig` + reference date). Frontend: show "Opens on `<date>` (in N days)" on pending tasks so the offset is no longer invisible.

**13f. Holds — reason trail, visibility, indication, LM view & notifications.**
- [x] `reason` field on `HoldRecord` (migration `leads.0010`); store the remark on the `TaskHold`/`LeadHold` row (not only ActivityLog).
- [x] Held Tasks menu shown to every user (API already self-scopes each caller to their held tasks); Held Leads stays on the lead-facing roles.
- [x] Lead list surfaces a "Task on hold" badge (`has_held_task` on the lead serializer).
- [x] Held Tasks page: non-assignee Lead Manager sees the hold **reason + full trail** (read-only, no Unhold); the assignee keeps Unhold. Task serializer exposes the hold trail.
- [x] Notify the Lead Manager (lead owner) when a task is **held** and when a task is **completed** (new `Notification.Type.TASK_COMPLETED`), alongside existing notifications.

**13g. Verification (done)** — **backend suite 145 tests pass** (6 new in `leads/tests.py`: successor assigned to the allocated Brown, Brown-edits/Red-views-only + Red 403 on task & lead edits, a White co-editing an execution task, Red **and** editor (Brown/White) both required to submit, hold reason stored on the interval + exposed on the held-tasks trail, hold/unhold trail accumulates with per-interval reasons, task-hold + task-completion notify the Lead Manager, and a pending task's `scheduled_open` open-date/days-out on the task serializer). Existing Phase-6/12 allocation walk + assignee tests updated to the Brown-edits model. Migration `leads.0010` (hold `reason` on lead/task + `TASK_COMPLETED` notification type) applies cleanly; `makemigrations --check` clean; `seed_workflows` re-seeded (17 tasks, execution tasks now `execution_brown`). Frontend `npm run build` + `oxlint` clean (only pre-existing fast-refresh warnings). Client-only pieces (resource table row-click/edit-icon, allocation-dropdown popper fix, pending-task "opens on <date>" banner, Held-Tasks reason+trail with Unhold gated to actors, leads-list "Task on hold" badge, held-tasks menu opened to all) verified by build/lint + code review.

### Phase 14 — PRD v3 / Tech Req v16 alignment

Executed 2026-07-16 per the reconciliation section's C-list and D-decisions. **Completed.**

- [x] **14a — Red executes (C13/D1).** All 10 execution-task `assignee` cells in [workflow_data.py](backend/leads/workflow_data.py) flipped `execution_brown` → `execution_red`; `engine._resolve_assignee` resolves `execution_red` via `resources.latest_execution_red` (Brown branch removed); the Phase-13 helpers (`latest_task_assignee`, `current_allocation_editor_ids`, `lead_allocation_people_ids`, `user_allocation_q`) deleted from [resources.py](backend/leads/resources.py); `can_view_task`/`can_edit_task` back to assignee-only (+ Lead Admin override, LM/lead-owner view); the allocation-people scoping removed from `lead_scope_q`/`_scoped_tasks`; `submit_allocation` requires only the Execution Red (12b's Red-mandatory kept, 13a's editor rule dropped). Frontend: allocation dialog drops the Brown/White-required gating, labels updated. The 5 Phase-13a tests rewritten to the Red model (incl. a new "allocated Brown/White can no longer view/edit" test).
- [x] **14b — Task 7/8 re-branching (C1).** `moved_to_next_stage` ("Has project moved to the next stage?", `required_when` re-pres = No) added to tasks 7 & 8; three-way routing seeded (Yes→8; No+Yes→**9 and 10 in parallel**; No+No→**17**); Task 9 → terminal. Engine `when` now accepts a **list of AND-ed conditions** (Tech Req §4.11 v15) via `_matched_route`. Frontend needed no change (`required_when` conditional rendering already generic). New tests: short-circuit to 17, 9+10 parallel open, `moved` required only when re-pres = No.
- [x] **14c — skipped + dropped statuses (C2/D4/D7).** `Task.Status` gains `SKIPPED` and `DROPPED` (migration `leads.0011`). Routing rules carry a data-driven `skip` list (5=No→[6–9], 7/8 No-paths→[8], 13=No→[14–16]); `engine._materialize_skips` creates checklist-less skipped rows **only when the lead has no instance of that task_no** (repeat cycles keep their closed rows); lead completion sweeps remaining `pending`→`skipped`. `pending` rows hidden from the lead task list (supersedes 13e's pending banner — `scheduled_open` stays on task detail); `progress`/dashboard denominators exclude skipped. Frontend: skipped/dropped badges + stepper banners.
- [x] **14d — remarks (C3/C4).** `leads.drop_remark`, `lead_hold.unhold_reason`, `task_hold.unhold_reason` (migration `leads.0012`). New `POST /api/leads/<id>/drop/` (actor = `can_drop_lead`: hold-lead actors ∪ Marketing creator) stores the remark, cascades open/held tasks → `dropped`, logs activity; direct `status=Dropped` PATCH now **rejected** (mirrors the On-Hold guard). Unhold (lead + task) stores its remark on the interval and the lead-level cascade copies it. Serializers expose `drop_remark`, `active_hold` (banner payload), and `unhold_reason` in the hold trail. Frontend: LeadDetail's status dropdown replaced by a **Drop button + popup**; amber on-hold banner + red dropped banner on LeadDetail; active-hold reason banner on a held task in the stepper. (Hold/unhold popups already captured remarks.)
- [x] **14e — resource module (C6/C7/C8).** `project_member6`–`10` (migration `leads.0013`, serializer/form pick them up via `RESOURCE_FIELDS`/`SINGLE_FIELDS`); `is_under_allocated` per-belt property + serializer flag + amber indicator (live in the form and on submitted rows); Task 16 `on_close` gains `close_superseded_allocations` (closes every non-closed Impl/Ext row except the current cycle's), Task 17 keeps `close_allocations` for the final row; new Extension rows **prefilled** from the previous cycle at Task-15 open (`_prefill_extension_row`). Tests: prefill contents, T16 supersede, two-cycle handover + final close, under-allocation flag.
- [x] **14f — reference status (C9).** `status` (active/inactive, default active) on `Country`/`Industry`/`Area` (shared abstract) and `Belt` (migrations `reference.0002`, `authentication.0005`); all four dropdown list endpoints filter to active; admin gains status column/filter/edit. New `reference/tests.py` covers active-only filtering.
- [x] **14g — leads list + cleanup (C5/C10/C12/P2 + doc hygiene).** `LeadSerializer` gains `task_progress {total, closed, percent}` and `current_task` (lowest open/hold task); [LeadsList.jsx](frontend/src/pages/LeadsList.jsx) rebuilt to the §5.18 spec — status-coloured Tracker bar with closed/total · % and "Not started", per-column filter row (Company/Project + Project ID text; Industry/Domain/Owner incl. "Not Assigned"/Current Task/Status dropdowns from on-screen data; AND; header "Clear filters"). Sidebar groups **Hold Leads/Hold Tasks under a "Hold Items"** label. Engine validation errors now quote the **display label** (`“Manpower required — Brown”: Negative values are not allowed.`). Duplicate `employee_id` message set on the model field ("This Employee ID is already in use by another user.", migration `authentication.0006`). Docs: PRD §1/§9 stale `LMS_Technical_Requirements.md` refs re-fixed; dated **D2 follow-up-broadening notes** added to PRD §5.11 + Tech Req §8; Tech Req §6's "My Tasks page" clause annotated void per D3.

**Verification (Phase 14):** backend suite **206 tests pass** project-wide (26 new across 14a–14f); `makemigrations --check` clean after `leads.0011–0013`, `reference.0002`, `authentication.0005–0006`; all four seed commands idempotent (17 tasks re-seeded twice through the phase). Frontend `npm run build` + `oxlint` clean (only pre-existing fast-refresh warnings). **Live HTTP smoke** against `runserver` (JWT, stdlib client): create lead → Task 1 (tracker 0/1) ✓; negative number → label-quoting 400 ✓; RM submit-without-Red 400, under-allocation flag true, submit-with-Red → Task 3 assigned to the Red ✓; 5=No → 10 with 6–9 `skipped`, hidden-pending list, tracker 4/6·67% + current task ✓; hold/unhold remarks stored + cascaded onto the task trail + `active_hold` payload ✓; direct `status=Dropped` 400, drop endpoint stores remark + cascades tasks → `dropped` ✓. Smoke users/lead removed afterward.

### Post-Phase-14 fixes

- [x] **Trigger already-due edge (2026-07-20).** A trigger-gated task (2/6/11/13/15) whose offset date had *already arrived* when its predecessor closed — e.g. Task 13's 60-day offset with a Task-12 engagement end date under 2 months out — was still created `pending`: unassigned, invisible to the Execution Red, and stuck until the next `open_due_tasks` cron run. `engine.complete_task` now evaluates the trigger at creation (`_active_trigger_config` + `_trigger_already_due`, reused by `run_due_triggers`) and opens the successor immediately when due; only a genuinely future open date pends. Tests: new `test_already_due_trigger_opens_immediately`; 3 scheduler tests adjusted to keep their original intent (offsets no longer due at creation). Full suite: **207 pass**. **Decision (2026-07-20):** when the end date is inside the window, Task 13 opens immediately even if `actual_start_date` is still in the future — a floor at the engagement start date was considered and explicitly rejected by the user.
- [x] **Login always lands on the dashboard (2026-07-20).** Logging out on a role-scoped page (e.g. a lead detail) stored that page as `state.from`, and the next login navigated back to it — showing "not assigned"-type screens instead of the dashboard. The `from` restore is removed: `Login.jsx` always navigates to `/dashboard` and `RequireAuth` no longer passes `state.from`. (The stale-cache half of this was already handled by `queryClient.clear()` on login/logout.)
- [x] **Project Closure shows all allocated Whites (2026-07-20).** The backend `ProjectDetailsSerializer` sends `whites` (a list — White is multi-select), but `ProjectClosure.jsx` read `r.white?.name` (singular, never present), so the White column was always "—" while Exec Red/Brown rendered fine. Column now renders every allocated White comma-joined (`names(r.whites)`), matching `LeadResourcesTab`. Allocation-dialog dropdown-vs-dialog dismissal guards (single `Select position="popper"` + `isInPortalLayer`; multi-select `Popover modal`) re-checked and intact.
- [x] **Task-step date fields allow past dates (2026-07-20).** **Decision (2026-07-20, per the user):** **every** task date field in every step — scalar and rowgroup cells alike (e.g. Engagement Start Date, invoice dates) — accepts past dates with **no floor at all**, an explicit exemption from the global "no past dates" rule (Tech Req §3). Supersedes both the Phase-11 `expected_start_date`-only exemption and the same-day lead-creation backfill floor (`engine._date_floor` / `lead_created_date` plumbing removed). Backend `_validate_scalar` now only checks date well-formedness; frontend task-step date pickers carry no `min` (`dateMinFor` removed). Non-task date fields (e.g. follow-up date) keep the global rule. Tests: `test_past_dates_allowed_on_any_task_date_field`, `test_rowgroup_date_cells_allow_past_dates`, `test_draft_save_rejects_malformed_date` (replacing the past-date-rejection tests). Full suite: **208 pass**; frontend build + oxlint clean.
- [x] **`mobile_no` tightened to exactly 10 digits (2026-07-21).** **Decision (2026-07-21, per the user):** supersedes the Phase-2 "numeric ≥0" treatment of `mobile_no` (no such rule existed in the docs — this is a new one, now recorded in Tech Req §3 and §4.1 / PRD §5.1). Field converted from `PositiveBigIntegerField` back to `CharField(max_length=10)` with a `^\d{10}$` `RegexValidator` (a plain integer can't preserve leading zeros or bound digit count) — migration `authentication.0007` first placeholders (`9999999999`) the 5 pre-existing rows that weren't 10 digits (ids 5/26/37/39/49; real numbers need re-entry via the Users UI), then alters the column. `UserSerializer.validate_mobile_no` added for a friendly message (mirrors `validate_date_of_joining`). Frontend `UserForm.jsx`: input switched from `type="number"` to a digit-filtered `type="text"` (`inputMode="numeric"`, hard-capped at 10 chars), submit gated on `mobileNoOk`, inline error text matching the joining-date pattern. Full suite: **208 pass**; frontend build clean.

### Phase 17 — Resource-allocation continuity, prefill & reassignment cascade

Requested by the user 2026-07-24, none of this was in the PRD/Tech Req before now. **Completed 2026-07-24.**

- [x] **Execution Red cannot be left empty once first assigned.** `ResourceAllocationSerializer.validate_execution_red` ([serializers.py](backend/leads/serializers.py)) rejects a PATCH that clears an already-non-null `execution_red` back to `None` — the Resource Manager can swap *who* it is but never blank it once set. Applies to **Execution Red only** (Brown/White stay optional, unchanged). The pre-existing mandatory-Red-at-submit rule (Phase 12/14a, unconditional on every stage including the first) already covered the "not empty at submit" half; this closes the remaining gap on the PATCH/edit path.
- [x] **Prefill on stage advance.** `resources._prefill_from_previous_cycle` (renamed/generalized from the Phase 14e Extension-only `_prefill_extension_row`) now runs for every allocation row, not just Extension — it copies every `SINGLE_RESOURCE_FIELDS` value + `whites` from the lead's most recently created prior `resource_allocation` row (2HR→SNT, SNT→Implementation, Implementation/Extension→Extension). A lead's first-ever row (2HR) has no predecessor, so it still opens blank. Test: `test_snt_and_implementation_rows_prefilled_from_previous_stage`.
- [x] **Reassignment cascade + notification.** `resources.reassign_execution_red`, called from `ResourceAllocationDetailView.perform_update` ([views.py](backend/leads/views.py)) whenever a PATCH changes `execution_red` from one non-null user to a different one: every task on that lead currently `open`/`hold`/`pending` and assigned to the outgoing Red moves to the incoming Red in the same request; closed/skipped tasks keep their historical assignee. Both the outgoing and incoming Red get a `task_reassigned` notification (existing Phase 8 type, no new type needed); the swap is logged to the lead's activity log. Test: `test_execution_red_reassignment_cascades_open_tasks_and_notifies`.
- [x] **Role-filtered allocation dropdowns.** `AllocationUserListView` ([views.py](backend/leads/views.py)) accepts `?field=execution_red|execution_brown|whites` and filters to users whose `belt` **or** `acting_belt_level` matches that role + its Potential tier (`ALLOCATION_FIELD_BELTS`); omitted/unrecognised `field` keeps the prior unfiltered list (auditors/project members). Frontend: `useAllocationUsers(field)` fetches three scoped lists in [Resources.jsx](frontend/src/pages/Resources.jsx), each merged with the currently-selected person if their belt no longer matches (`withCurrentSelection`) so an existing pick never silently disappears. Test: `test_allocation_users_filtered_by_belt`.

**Verification (Phase 17):** backend suite **220 tests pass** project-wide (12 new: belt-filtered dropdowns, Red-cannot-be-cleared 400, reassignment cascade + both-sides notification, SNT/Implementation prefill). No migration needed (no schema change). Frontend `npm run build` + `npx oxlint` clean (only the pre-existing fast-refresh warnings).

## 4. Verification
Each phase carries its own end-to-end verification when executed: migrations apply cleanly, seed commands run idempotently, endpoints are exercised against the running Django dev server (auth + permission scoping checked per role), and the corresponding frontend flow is driven against the live API (not the mock). Phase 2 specifically requires re-verifying the existing auth flow after the username switch. The workflow engine (Phase 4) is verified by walking a lead through the full 17-task sequence including each branch (Task 5 skip, 7/8 loop, extension cycle) and confirming Project-ID/`project_details`/resource auto-close behavior in Phase 6.
